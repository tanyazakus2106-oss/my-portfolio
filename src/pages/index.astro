---
import BaseLayout from "../layouts/BaseLayout.astro";
import ProjectCard from "../components/ProjectCard.astro";
import ParticlesBg from "../components/ParticlesBg.astro";
import { getCollection } from "astro:content";

const projects = (await getCollection("projects")).sort(
  (a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf(),
);

const eyebrowText = "Open to full-time & freelance";
const headlineText = "Designing clarity into complex software products";
const descriptionText =
  "Senior Product Designer with 6+ years of experience on complex B2B SaaS platforms. Currently exploring AI design engineering.";

const eyebrowWordsList = eyebrowText.split(" ");
const headlineWordsList = headlineText.split(" ");
const descriptionWordsList = descriptionText.split(" ");
---

<BaseLayout
  title="Tanya Zakus — UX/UI Designer"
  description="Senior Product Designer with 6+ years on complex B2B SaaS platforms, now exploring AI design engineering. Selected case studies and current availability."
  ogImage="/og-image.png"
>
  {/* Hero section */}
  <section
    class="relative min-h-screen min-h-svh -mt-20 pt-20 flex items-center overflow-hidden"
  >
    <ParticlesBg />
    <div class="container w-full relative z-10">
      <div class="max-w-[760px]">
        <p
          class="text-sm uppercase tracking-[0.08em] font-normal text-[var(--color-text-secondary)]"
          data-line-group
        >
          <span class="animate-line-word" aria-hidden="true">
            <span class="status-dot"></span>
          </span>
          {
            eyebrowWordsList.map((word, idx) => (
              <>
                {idx > 0 && " "}
                <span class="animate-line-word">{word}</span>
              </>
            ))
          }
        </p>
        <h1
          class="mt-[var(--spacing-md)] text-[clamp(3rem,8vw,7rem)] leading-[0.95] text-[var(--color-text-primary)] max-w-[580px]"
          style="--block-offset: 600ms"
          data-line-group
        >
          {
            headlineWordsList.map((word, idx) => (
              <>
                {idx > 0 && " "}
                <span class="animate-line-word">{word}</span>
              </>
            ))
          }
        </h1>
        <p
          class="mt-[24px] text-[20px] leading-[30px] text-[var(--color-text-secondary)] max-w-[580px]"
          style="--block-offset: 1410ms"
          data-line-group
        >
          {
            descriptionWordsList.map((word, idx) => (
              <>
                {idx > 0 && " "}
                <span class="animate-line-word">{word}</span>
              </>
            ))
          }
        </p>
      </div>
    </div>
  </section>

  {/* Divider between hero and selected work */}
  <div class="container">
    <hr class="border-t border-[var(--color-border)]" />
  </div>

  {/* Projects section */}
  <section
    id="projects"
    class="pt-[var(--spacing-section)] pb-[var(--spacing-section)] scroll-mt-20"
  >
    <div class="container">
      <h2
        class="text-[46px] md:text-[48px] text-[var(--color-text-primary)] mb-[var(--spacing-2xl)]"
      >
        Projects
      </h2>
      <div class="flex flex-col gap-[var(--spacing-section)]">
        {projects.map((entry, i) => <ProjectCard entry={entry} index={i} />)}
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import "../scripts/scroll-animation.ts";
  import "../scripts/hero-line-animation.ts";
</script>
