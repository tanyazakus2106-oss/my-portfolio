---
phase: quick
plan: 260506-zm5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/[id].astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "Both prev/next links use hover:text-[var(--color-accent)] (not text-primary)"
    - "Label text 'Previous project' / 'Next project' is wrapped in a relative span with after-pseudo sweep-underline on hover"
    - "Arrows (← →) sit OUTSIDE the underline span — underline only sweeps under the label text"
    - "Underline animation matches the ProjectCard 'View project' pattern: origin-right → scale-x-0 → group-hover origin-left → scale-x-100, 300ms ease-in-out"
  artifacts:
    - path: "src/pages/projects/[id].astro"
      provides: "Restored sweep-underline hover states on prev/next nav"
      contains: "group-hover:after:scale-x-100"
  key_links:
    - from: "case study prev/next nav"
      to: "ProjectCard 'View project' link"
      via: "shared underline-sweep micro-interaction"
      pattern: "after:origin-bottom-right"
---

<objective>
Restore the sweep-underline-on-hover micro-interaction and accent hover color on the prev/next case study navigation. The structural redesign in 260506-zm4 dropped these in favor of a simpler text-color-only hover; the user wants them back to match the rest of the site's hover language.

Out of scope: layout, typography, container widths, font sizes — all unchanged.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260506-zm4-redesign-project-page-grid/260506-zm4-SUMMARY.md
</context>

<tasks>

### Task 1: Restore prev/next hover states

**Files:** `src/pages/projects/[id].astro`

**Action:**
1. Change `hover:text-[var(--color-text-primary)]` → `hover:text-[var(--color-accent)]` on both prev/next anchor tags.
2. Wrap "Previous project" in a `<span class="relative after:absolute after:bg-current after:bottom-0 after:left-0 after:h-px after:w-full after:origin-bottom-right after:scale-x-0 group-hover:after:origin-bottom-left group-hover:after:scale-x-100 after:transition-transform after:ease-in-out after:duration-300">` with the arrow `←` *outside* the wrapping span.
3. Same for "Next project" with arrow `→` *after* the wrapping span.

**Verify:**
- `grep -c "hover:text-\[var(--color-accent)\]" src/pages/projects/\[id\].astro` returns at least 2.
- `grep -c "group-hover:after:scale-x-100" src/pages/projects/\[id\].astro` returns 2.
- `grep -c "after:origin-bottom-right" src/pages/projects/\[id\].astro` returns 2.

**Done when:**
- Hover over either prev or next link in the browser:
  - Text color shifts to accent
  - Underline sweeps under the label text only (not under the arrow)
  - 300ms ease-in-out feel matches the homepage `ProjectCard` "View project" link

</tasks>
