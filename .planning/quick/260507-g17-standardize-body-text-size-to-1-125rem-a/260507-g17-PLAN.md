---
quick_id: 260507-g17
type: quick
mode: execute
autonomous: true
files_modified:
  - src/styles/global.css
  - src/pages/projects/[id].astro
  - src/components/ProjectCard.astro
  - src/components/FeaturedCard.astro
must_haves:
  truths:
    - "Body element font-size is 1.125rem (18px) globally"
    - "Case-study MDX paragraphs render at 1.125rem (18px), not 1rem"
    - "Project card and featured card descriptions render at 18px via the Tailwind text-lg utility"
    - "Lede paragraphs (home hero description, case-study summary) remain visually larger and untouched"
    - "npm run typecheck remains clean"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Body font-size: 1.125rem; line-height: 1.6 on the body rule"
      contains: "font-size: 1.125rem"
    - path: "src/pages/projects/[id].astro"
      provides: ".case-prose p font-size: 1.125rem inside <style is:global>"
      contains: "font-size: 1.125rem"
    - path: "src/components/ProjectCard.astro"
      provides: "Description paragraph using text-lg utility"
      contains: "text-lg"
    - path: "src/components/FeaturedCard.astro"
      provides: "Description paragraph using text-lg utility"
      contains: "text-lg"
  key_links:
    - from: "src/styles/global.css body rule"
      to: "every page (cascading default)"
      via: "global stylesheet loaded by BaseLayout"
      pattern: "font-size: 1.125rem"
    - from: ".case-prose p rule"
      to: "MDX prose paragraphs in src/content/projects/**/*.mdx"
      via: "<article class=\"... case-prose\"> wrapper in [id].astro"
      pattern: "case-prose p"
---

<objective>
Standardize the design system's "body text" size to **1.125rem (18px)** across every paragraph-of-running-text surface. Today three different sizes coexist:

- `body { font-size: 20px }` in `global.css` (too large for body — bleeds into content not styled by a more specific rule)
- `.case-prose p { font-size: 1rem }` in `[id].astro` (case-study MDX paragraphs at 16px — too small)
- `text-[18px] leading-[28px]` on `ProjectCard` / `FeaturedCard` description (already the target value, but expressed as an arbitrary value rather than the canonical `text-lg` utility)

After this change, the body default, the case-study prose, and the card descriptions all render at 18px. The two **lede** paragraphs are intentionally left larger to preserve editorial hierarchy.

**Purpose:** Single canonical body size; eliminates inconsistency that a future contributor (or Tanya months later) would have to re-derive from three sources.

**Output:** Four surgical edits in four files. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<!-- Current state of each file the executor will touch — extracted so no re-reads are needed -->

<current_state>
**src/styles/global.css (lines 78–84):**
```css
body {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  letter-spacing: -0.01em;
  font-size: 20px;
  line-height: 28px;
}
```

**src/pages/projects/[id].astro (lines 131–137, inside `<style is:global>`):**
```css
.case-prose p {
  font-size: 1rem;
  line-height: 1.625;
  color: var(--color-text-secondary);
  margin-top: var(--spacing-md);
  margin-bottom: var(--spacing-md);
}
```

**src/components/ProjectCard.astro (line 57):**
```astro
<p class="text-[18px] leading-[28px] text-[var(--color-text-secondary)]">
  {entry.data.summary}
</p>
```

**src/components/FeaturedCard.astro (line 56):**
```astro
<p class="text-[18px] leading-[28px] text-[var(--color-text-secondary)]">
  {entry.data.summary}
</p>
```
</current_state>

<tailwind_v4_note>
Tailwind v4's `text-lg` utility ships with `font-size: 1.125rem` and `line-height: 1.75rem` (28px) as its default. That matches the existing `text-[18px] leading-[28px]` exactly — visual rendering on the cards is unchanged; only the class names get tidier.
</tailwind_v4_note>

<do_not_touch>
The following surfaces are **explicitly excluded** from this change. Do not modify them.

1. **`src/pages/index.astro:42`** — home hero description
   `text-[20px] leading-[30px]` — this is the lede; stays larger by design.
2. **`src/pages/projects/[id].astro:57`** — case-study summary (the lede paragraph that sits between the title and the hero image)
   `text-xl md:text-2xl` — also a lede; stays larger.
3. **Microcopy / labels / metadata** — anything currently at `text-xs`, `text-sm`, `text-base`, `text-[14px]`, etc. (role labels on cards, timeline `<dt>/<dd>`, prev/next nav titles, footer text). These are not "body text" — they are smaller utilities used intentionally.
4. **Headings** — `h1`/`h2`/`h3`/`h4` rules (including `.case-prose h2` and `.case-prose h3`).
5. **Line-height on `.case-prose p`** — keep `line-height: 1.625` (this is the case-study prose reading rhythm, separate from the body default).
</do_not_touch>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize body text size to 1.125rem across the four surfaces</name>
  <files>src/styles/global.css, src/pages/projects/[id].astro, src/components/ProjectCard.astro, src/components/FeaturedCard.astro</files>
  <action>
Make exactly these four edits — no more, no less. Use the Edit tool for each.

**Edit 1 — `src/styles/global.css` (body rule, lines 82–83):**
- Change `font-size: 20px;` → `font-size: 1.125rem;`
- Change `line-height: 28px;` → `line-height: 1.6;`
- Leave the three preceding declarations (`-webkit-font-smoothing`, `-moz-osx-font-smoothing`, `letter-spacing`) untouched.

**Edit 2 — `src/pages/projects/[id].astro` (`.case-prose p` rule inside the `<style is:global>` block, line 132):**
- Change `font-size: 1rem;` → `font-size: 1.125rem;`
- **Leave `line-height: 1.625;` untouched** (case-study prose keeps its tighter rhythm).
- Leave `color`, `margin-top`, `margin-bottom` untouched.

**Edit 3 — `src/components/ProjectCard.astro` (line 57):**
- Replace the substring `text-[18px] leading-[28px]` with `text-lg`.
- Keep `text-[var(--color-text-secondary)]` and any other classes on the same line — only the two arbitrary-value classes change.
- Final line should read: `<p class="text-lg text-[var(--color-text-secondary)]">`

**Edit 4 — `src/components/FeaturedCard.astro` (line 56):**
- Same substitution as Edit 3: `text-[18px] leading-[28px]` → `text-lg`.
- Final line should read: `<p class="text-lg text-[var(--color-text-secondary)]">`

Do not touch any other lines, files, or classes. Do not "tidy up" adjacent code. Do not modify the `do_not_touch` list above.
  </action>
  <verify>
    <automated>
npm run typecheck \
  && test "$(grep -c 'font-size: 20px' src/styles/global.css)" = "0" \
  && grep -n 'font-size: 1.125rem' src/styles/global.css \
  && grep -A2 'case-prose p' src/pages/projects/\[id\].astro | grep 'font-size: 1.125rem' \
  && test "$(grep -E 'text-\[18px\]' src/components/ProjectCard.astro src/components/FeaturedCard.astro | wc -l | tr -d ' ')" = "0" \
  && grep -n 'text-lg' src/components/ProjectCard.astro \
  && grep -n 'text-lg' src/components/FeaturedCard.astro \
  && grep -n 'text-\[20px\] leading-\[30px\]' src/pages/index.astro \
  && grep -n 'text-xl md:text-2xl' src/pages/projects/\[id\].astro
    </automated>
  </verify>
  <done>
- `npm run typecheck` exits 0.
- `grep -n "font-size: 20px" src/styles/global.css` returns zero matches.
- `grep -n "font-size: 1.125rem" src/styles/global.css` shows the body rule (around line 82).
- `grep -A2 "case-prose p" src/pages/projects/[id].astro` shows `font-size: 1.125rem` (line ~132); `line-height: 1.625` still present.
- `grep -E "text-\[18px\]" src/components/ProjectCard.astro src/components/FeaturedCard.astro` returns zero matches.
- `grep -n "text-lg" src/components/ProjectCard.astro` shows line 57; same grep on FeaturedCard.astro shows line 56.
- The home hero description (`index.astro:42`) still shows `text-[20px] leading-[30px]` — untouched.
- The case-study summary (`[id].astro:57`) still shows `text-xl md:text-2xl` — untouched.
  </done>
</task>

</tasks>

<verification>
Run the automated verification block in Task 1. All eight checks must pass before commit. If any of the "do not touch" greps disappear, revert immediately — the executor has overreached.

Visual sanity check (optional, owner runs locally):
- `npm run dev`, open `/` (home), `/work`, and any `/projects/<slug>` page.
- Home hero description should still feel larger than the project card descriptions (it does — 20px vs 18px).
- Case-study body paragraphs should feel slightly larger than before (16px → 18px).
- Project card / featured card descriptions should look identical to before (already 18px; only class names changed).
</verification>

<success_criteria>
- All eight automated grep / typecheck assertions in Task 1 pass.
- Exactly four files modified; no others.
- Commit message: `refactor(typography): standardize body text size to 1.125rem across design system`
</success_criteria>

<output>
After completion, create `.planning/quick/260507-g17-standardize-body-text-size-to-1-125rem-a/260507-g17-SUMMARY.md` documenting the four edits, before/after for each, and confirmation that the lede paragraphs and microcopy were left untouched.
</output>
