---
quick_id: 260618-rd4
slug: reduce-nav-tab-underline-height-to-1px
description: Reduce nav tab underline height to 1px
date: 2026-06-18
status: complete
---

# Quick Task 260618-rd4: Reduce nav tab underline height to 1px

## Goal

Thin the desktop nav tab accent underline from 2px to 1px (hairline), matching the ArrowLink.

## Task

1. **Edit `src/components/Header.astro`** — change the underline `<span>`'s `after:h-[2px]` to `after:h-px`.
   - files: `src/components/Header.astro`
   - action: Swap the height utility on the nav underline `::after`.
   - verify: `npm run typecheck` passes; underline renders 1px for both active and hover states.
   - done: Nav underline is 1px.

## Scope notes

- `after:h-px` is Tailwind's literal 1px utility; now identical to ArrowLink's `after:h-px`.
