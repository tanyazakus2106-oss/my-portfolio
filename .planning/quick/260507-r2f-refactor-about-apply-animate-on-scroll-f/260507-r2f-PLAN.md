---
quick_id: 260507-r2f
slug: refactor-about-apply-animate-on-scroll-f
status: complete
description: apply animate-on-scroll fade-up to about page sections (matches tushar.work)
files_modified:
  - src/pages/about.astro
autonomous: true
---

# Quick Task 260507-r2f: About — apply existing scroll-fade animation

## Objective

Apply the project's existing `animate-on-scroll` entrance animation to the About page sections. tushar.work/about uses the same fade-up pattern (`opacity:0; translateY(40px)` → `opacity:1; translateY(0)`), and this project already has it implemented in `src/scripts/scroll-animation.ts` + `src/styles/global.css` — used today on home page project cards.

## Tasks

1. Add `animate-on-scroll` class to the hero grid div (line 15)
2. Add `animate-on-scroll` class to the "How I work" section (line 47)
3. Add `animate-on-scroll` class to the "Beyond work" section (line 62)
4. Import the scroll-animation script: add `<script>import '../scripts/scroll-animation.ts';</script>` after `</BaseLayout>`

## Verification

- `npm run build`: 7 pages built
- Visual: scroll triggers fade-up entrance on each section as it enters the viewport, with the project's standard 70ms stagger and 500ms ease-out duration

## Existing animation spec (no new code added)

| Property | Value |
|----------|-------|
| Initial opacity | 0 |
| Initial transform | `translateY(24px)` |
| Final opacity | 1 |
| Final transform | `translateY(0)` |
| Duration | 500ms |
| Easing | ease-out |
| Stagger between elements | 70ms (`calc(var(--stagger-index) * 70ms)`) |
| Reduced motion | Reveals immediately, no transition |
| Threshold | IntersectionObserver fires at 5% visibility |
| Fallback | 2.5s safety timeout reveals any non-fired elements (high-zoom edge case) |
