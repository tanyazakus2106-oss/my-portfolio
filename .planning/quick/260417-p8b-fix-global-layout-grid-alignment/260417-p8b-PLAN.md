---
phase: quick
plan: 260417-p8b
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - src/pages/work.astro
  - src/pages/projects/[id].astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "Header content, page content, dividers, and footer content all share the same left and right edge at every breakpoint"
    - "Horizontal padding scales correctly: 1.5rem mobile, 2rem tablet (768px+), 4rem desktop (1440px+)"
    - "Container is centered with auto margins and a max-width of 1200px"
    - "No inline padding/max-width duplication remains — all alignment comes from .container"
  artifacts:
    - path: "src/styles/global.css"
      provides: ".container utility class definition"
      contains: ".container"
    - path: "src/components/Header.astro"
      provides: "Header inner div using .container"
    - path: "src/components/Footer.astro"
      provides: "Footer inner div using .container"
    - path: "src/pages/index.astro"
      provides: "Hero, divider, selected work sections using .container"
    - path: "src/pages/work.astro"
      provides: "Work page wrapper using .container"
    - path: "src/pages/projects/[id].astro"
      provides: "Project page wrapper using .container"
  key_links:
    - from: "src/styles/global.css (.container)"
      to: "Header.astro, Footer.astro, index.astro, work.astro, [id].astro"
      via: "CSS class applied to inner wrapper divs"
      pattern: "class=\"container"
---

<objective>
Define a single `.container` CSS utility class in global.css, then replace the duplicated inline `max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]` pattern everywhere it appears with that class.

Purpose: A single source of truth for horizontal layout alignment eliminates drift between header, content areas, dividers, and footer as the site grows.
Output: All layout elements share identical horizontal bounds — visually aligned at 375px, 768px, and 1440px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
</context>

<interfaces>
<!-- Current duplicated pattern being replaced — present in 6 files -->
<!-- Header.astro line 21: class="max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)] h-full flex items-center justify-between" -->
<!-- Footer.astro line 14: class="max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]" -->
<!-- index.astro hero section line 15: px-[...] on section, max-w-[1200px] mx-auto w-full on inner div -->
<!-- index.astro divider line 27: max-w-[1200px] mx-auto px-[...] -->
<!-- index.astro selected work line 32: px-[...] on section, max-w-[1200px] mx-auto on inner div -->
<!-- work.astro line 11: max-w-[1200px] mx-auto px-[...] -->
<!-- [id].astro line 43: max-w-[1200px] mx-auto px-[...] -->

Spacing tokens in global.css:
  --spacing-lg: 1.5rem   (24px — mobile padding)
  --spacing-2xl: 3rem    (48px — tablet padding)
  --spacing-3xl: 4rem    (64px — desktop padding)

Constraint: responsive padding per task description is 1.5rem / 2rem / 4rem.
  --spacing-xl: 2rem matches the 2rem tablet target.
  Current code uses --spacing-2xl (3rem) at md breakpoint — override to --spacing-xl (2rem) per spec.
  Desktop (lg) stays --spacing-3xl (4rem). Correct.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define .container utility class in global.css</name>
  <files>src/styles/global.css</files>
  <action>
Add a `.container` class at the end of global.css (after the scroll animation block). The class must:

```css
/* ─── Layout container — shared max-width + responsive padding ─── */
.container {
  width: 100%;
  max-width: 1200px;
  margin-left: auto;
  margin-right: auto;
  padding-left: var(--spacing-lg);   /* 1.5rem mobile */
  padding-right: var(--spacing-lg);
}

@media (min-width: 768px) {
  .container {
    padding-left: var(--spacing-xl);  /* 2rem tablet */
    padding-right: var(--spacing-xl);
  }
}

@media (min-width: 1440px) {
  .container {
    padding-left: var(--spacing-3xl); /* 4rem desktop */
    padding-right: var(--spacing-3xl);
  }
}
```

Do not use Tailwind `@apply` — write plain CSS so it is readable and not Tailwind-version-dependent.
Do not change any existing rules in the file.
  </action>
  <verify>
    <automated>grep -n "\.container" /Users/tanyazakus/Desktop/my-portfolio/src/styles/global.css</automated>
  </verify>
  <done>.container class exists in global.css with max-width: 1200px, auto margins, and the three responsive padding breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicated alignment pattern with .container across all files</name>
  <files>
    src/components/Header.astro,
    src/components/Footer.astro,
    src/pages/index.astro,
    src/pages/work.astro,
    src/pages/projects/[id].astro
  </files>
  <action>
Apply the following replacements file by file. Keep all OTHER classes on each element intact — only remove the alignment-specific classes listed.

**src/components/Header.astro — line 21**
Remove from the inner div: `max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]`
Add: `container`
Resulting class list: `container h-full flex items-center justify-between`

**src/components/Footer.astro — line 14**
Remove from the inner div: `max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]`
Add: `container`

**src/pages/index.astro**

Hero section (lines 15–24): The pattern here splits padding onto the `<section>` and max-width onto the inner div. Consolidate into the inner div.
- On `<section>`: remove `px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]` (leave `min-h-[800px] min-h-svh flex items-center`)
- On inner `<div>`: replace `max-w-[1200px] mx-auto w-full` with `container w-full`

Divider wrapper (line 27): replace `max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]` with `container`

Selected Work section (lines 32–45): The pattern again splits padding onto `<section>` and max-width onto inner div.
- On `<section>`: remove `px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]` (leave vertical padding `pt-[var(--spacing-3xl)] pb-[var(--spacing-3xl)]`)
- On inner `<div>`: replace `max-w-[1200px] mx-auto` with `container`

**src/pages/work.astro — line 11**
Replace the wrapper div class: `max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)] pt-[var(--spacing-4xl)] pb-[var(--spacing-3xl)]`
With: `container pt-[var(--spacing-4xl)] pb-[var(--spacing-3xl)]`

**src/pages/projects/[id].astro — line 43**
Replace the outer wrapper div class: `max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)] pt-[var(--spacing-3xl)] pb-[var(--spacing-3xl)]`
With: `container pt-[var(--spacing-3xl)] pb-[var(--spacing-3xl)]`

After all edits, confirm zero occurrences of the old pattern remain:
`grep -r "max-w-\[1200px\]" src/`
Result must be empty.
  </action>
  <verify>
    <automated>grep -rn "max-w-\[1200px\]" /Users/tanyazakus/Desktop/my-portfolio/src/ || echo "CLEAN — no inline max-w-[1200px] found"</automated>
  </verify>
  <done>
    - grep finds zero occurrences of `max-w-[1200px]` in src/
    - grep finds zero occurrences of `px-[var(--spacing-lg)]` used for layout alignment in src/
    - All 6 files use `container` on their inner wrapper divs
    - `npx astro build` completes without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Single .container CSS class replacing duplicated inline alignment in Header, Footer, index, work, and project pages.
    Responsive padding: 1.5rem (mobile) / 2rem (768px+) / 4rem (1440px+). Max-width 1200px, centered.
  </what-built>
  <how-to-verify>
    Run `npm run dev` in the project root, then check each page at three viewport widths using browser DevTools (toggle responsive mode):

    **375px (mobile):**
    - / (home): header logo, hero text, divider rule, "Selected Work" heading, and footer "Let's work together" all share the same 1.5rem left indent
    - /work: "Work" heading left edge matches header logo left edge
    - /projects/[any-id]: case study back link left edge matches header logo

    **768px (tablet):**
    - All of the above now have 2rem left indent — content shifts rightward slightly from 375px
    - Divider rule left edge is flush with nav link "Work" in the header

    **1440px (desktop):**
    - All content has 4rem left indent
    - No element bleeds past 1200px center column

    If any element is noticeably misaligned from the others at any breakpoint, note which element and at which width.
  </how-to-verify>
  <resume-signal>Type "approved" if alignment is consistent at all three widths, or describe which element is off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class rename | Removing inline Tailwind classes and adding a plain CSS class — risk of missed occurrences causing misalignment on a specific page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p8b-01 | Tampering | Layout alignment | mitigate | Task 2 verify step runs grep to assert zero remaining occurrences of old pattern before marking done |
</threat_model>

<verification>
- `grep -rn "max-w-\[1200px\]" src/` returns no matches
- `npx astro build` exits 0
- Visual spot-check confirms header/content/footer share left edge at 375px, 768px, 1440px
</verification>

<success_criteria>
- One canonical .container class in global.css — no inline duplication
- Header, footer, all page content wrappers, and dividers visually align at identical horizontal bounds
- Responsive padding exactly matches spec: 1.5rem / 2rem / 4rem across the three breakpoints
- Build passes with no type or compilation errors
</success_criteria>

<output>
After completion, create `.planning/quick/260417-p8b-fix-global-layout-grid-alignment/260417-p8b-SUMMARY.md`
</output>
