---
phase: quick
plan: 260506-zm8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/[id].astro
  - src/components/ProjectCard.astro
  - src/components/FeaturedCard.astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "All four text+arrow affordances render in sentence case (no uppercase, no wide tracking)"
    - "All four use text-secondary as the default color, animating to accent on hover"
    - "Sweep-underline micro-interaction from prior tasks is preserved on all four sites"
    - "transition-colors is applied so color change animates smoothly (matches existing hover-color sites)"
  artifacts:
    - path: "src/pages/projects/[id].astro"
      provides: "Back link uses secondary→accent hover color; prev/next labels render as sentence case"
      contains: "hover:text-[var(--color-accent)]"
    - path: "src/components/ProjectCard.astro"
      provides: "View project uses group-hover:text-accent (was always accent)"
      contains: "group-hover:text-[var(--color-accent)]"
    - path: "src/components/FeaturedCard.astro"
      provides: "View project uses group-hover:text-accent"
      contains: "group-hover:text-[var(--color-accent)]"
  key_links: []
---

<objective>
Apply two changes uniformly across all text+arrow affordances:

1. **Sentence case** — remove `uppercase` and `tracking-[0.08em]` from the prev/next nav labels in `[id].astro`. (The other three already render in sentence case.)
2. **Secondary color default** — switch View project (in `ProjectCard` + `FeaturedCard`) and Back to work (in `[id].astro`) from always-accent to `text-secondary` default with hover→accent. Add `transition-colors` so the color change animates.

Sweep-underline behavior, sizes, and structural placement are unchanged.
</objective>

<context>
@.planning/STATE.md
@.planning/quick/260506-zm7-unify-back-link-pattern/260506-zm7-SUMMARY.md
</context>

<tasks>

### Task 1: Sentence case + secondary default across all text+arrow buttons

**Files:**
- `src/pages/projects/[id].astro` (3 sites: back link + prev label + next label)
- `src/components/ProjectCard.astro` (1 site: View project span)
- `src/components/FeaturedCard.astro` (1 site: View project span)

**Action:**

`[id].astro` back link — change:
- `text-base text-[var(--color-accent)]` → `text-base text-[var(--color-text-secondary)] hover:text-[var(--color-accent)] transition-colors`

`[id].astro` prev label span — change:
- `class="text-[13px] uppercase tracking-[0.08em]"` → `class="text-[13px]"`

`[id].astro` next label span — same as prev.

`ProjectCard.astro` View project span — change:
- `class="text-base text-[var(--color-accent)]"` → `class="text-base text-[var(--color-text-secondary)] group-hover:text-[var(--color-accent)] transition-colors"`

`FeaturedCard.astro` View project span — same as ProjectCard.

**Verify:**
- `grep -n "uppercase tracking-\[0.08em\]" src/pages/projects/\[id\].astro` returns no matches inside the prev/next nav (the eyebrow+metadata uppercase labels at the top of the page should remain).
- `grep -c "group-hover:text-\[var(--color-accent)\]" src/components/ProjectCard.astro src/components/FeaturedCard.astro` returns 2 (one per file).
- `grep -c "hover:text-\[var(--color-accent)\] transition-colors" src/pages/projects/\[id\].astro` returns at least 3 (back link + prev + next anchors).
- `grep -c "after:origin-bottom-right" src/pages/projects/\[id\].astro` still returns 3 (sweep-underline preserved).

**Done when:**
- "Previous project" / "Next project" render in normal sentence case (not all-caps), with default tracking.
- All four text+arrow affordances default to muted secondary text.
- Hovering any of them animates the color to accent and sweeps the underline simultaneously.
- The eyebrow at the top of the case study page (role + skills) keeps its uppercase tracking — that's a different element, not in scope.

</tasks>
