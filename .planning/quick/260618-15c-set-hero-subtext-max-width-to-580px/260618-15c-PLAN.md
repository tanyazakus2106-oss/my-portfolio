---
quick_id: 260618-15c
slug: set-hero-subtext-max-width-to-580px
description: Set hero subtext max-width to 580px
date: 2026-06-17
status: complete
---

# Quick Task 260618-15c: Set hero subtext max-width to 580px

## Goal

Constrain the homepage hero subtext line width to 580px.

## Task

1. **Edit `src/pages/index.astro`** — change the subtext `<p>` max-width from `max-w-[600px]` to `max-w-[580px]` (line ~64).
   - files: `src/pages/index.astro`
   - action: Swap the Tailwind arbitrary max-width on the description paragraph only.
   - verify: `npm run typecheck` passes; subtext wraps at the narrower measure.
   - done: Hero subtext caps at 580px.

## Scope notes

- Touch only the description `<p>` (line ~64). The headline `<h1>` (line ~50) keeps its own `max-w-[600px]`.
