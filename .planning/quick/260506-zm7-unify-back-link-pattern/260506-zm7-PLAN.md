---
phase: quick
plan: 260506-zm7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/[id].astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "'Back to work' link uses the same micro-interaction as 'View project': sweep-underline under text only, arrow OUTSIDE the underline span"
    - "Back link uses unicode arrow '←' (not SVG arrow with translate-x animation)"
    - "Back link uses text-base text-[var(--color-accent)] matching View project visual treatment"
    - "Sweep-underline classes match View project pattern verbatim"
  artifacts:
    - path: "src/pages/projects/[id].astro"
      provides: "Back to work link unified with View project pattern"
      contains: "after:origin-bottom-right"
  key_links:
    - from: "Back to work link"
      to: "ProjectCard 'View project' link, prev/next case-study nav"
      via: "shared sweep-underline + arrow-outside-span pattern"
      pattern: "after:origin-bottom-right"
---

<objective>
Unify the "Back to work" link's affordance pattern with the rest of the site. The View project link in `ProjectCard` / `FeaturedCard` and the prev/next nav (per zm5) all use a sweep-underline animation under the label text only, with the arrow outside the span. The Back to work link is the lone outlier — uses an SVG arrow with a translate-x hover animation, no sweep-underline.

Replace the SVG arrow + translate-x interaction with the unicode `←` + sweep-underline pattern. Use the View project visual treatment (text-base, accent color) since the Back to work is functionally a CTA-style link.

Out of scope: prev/next nav (settled in zm5+zm6), View project links (already canonical).
</objective>

<context>
@.planning/STATE.md
@.planning/quick/260506-zm6-restore-prev-next-typography/260506-zm6-SUMMARY.md
</context>

<tasks>

### Task 1: Replace Back to work SVG-arrow link with View project pattern

**Files:** `src/pages/projects/[id].astro`

**Action:**
Replace the current back link block:
```astro
<a href="/work" class="group inline-flex items-center gap-... text-sm text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)] ...">
  <svg class="w-4 h-4 transition-transform group-hover:-translate-x-1" ...arrow-left />
  Back to work
</a>
```

With:
```astro
<a href="/work" class="group inline-block text-base text-[var(--color-accent)] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--color-accent)]">
  ← <span class="relative after:absolute after:bg-current after:bottom-0 after:left-0 after:h-px after:w-full after:origin-bottom-right after:scale-x-0 group-hover:after:origin-bottom-left group-hover:after:scale-x-100 after:transition-transform after:ease-in-out after:duration-300">Back to work</span>
</a>
```

The `←` arrow sits OUTSIDE the underline span (so the sweep doesn't cross under the arrow), exactly mirroring how View project places `→` outside its underline span.

**Verify:**
- `grep -n "Back to work" src/pages/projects/\[id\].astro` shows the text inside a `<span class="relative after:...">` wrapper.
- `grep -c "after:origin-bottom-right" src/pages/projects/\[id\].astro` returns 3 (back link + prev + next).
- `grep -n "translate-x-1\|<svg.*Back\|stroke-linecap.*Back" src/pages/projects/\[id\].astro` returns no matches (SVG arrow gone).

**Done when:**
- Back link looks visually consistent with View project (accent color, 16px, sweep underline on hover).
- Hover triggers underline sweep under "Back to work" text only — arrow stays static.
- No translate-x on SVG (because there's no SVG anymore).

</tasks>
