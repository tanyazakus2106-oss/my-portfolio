---
quick_id: 260507-r5q
slug: refactor-about-remove-max-w-600px-cap-fr
status: complete
description: remove max-w-[600px] cap from sub-section paragraphs so body text fills the grid 3-col span
files_modified:
  - src/pages/about.astro
autonomous: true
---

# Quick Task 260507-r5q: Body text width aligns with grid

## Objective

Body paragraphs in "How I work" and "Beyond work" sections were capped at `max-w-[600px]`, forcing them narrower than the `lg:col-span-3` grid column they live in. tushar.work/about reference shows body text filling the full 3-col grid span. Remove the per-paragraph cap so the column itself drives line length.

## Task

Remove `max-w-[600px]` from 4 paragraph elements in `src/pages/about.astro`:
- 2 paragraphs in "How I work" section (lines 52, 55)
- 2 paragraphs in "Beyond work" section (lines 67, 70)

Hero intro paragraph (line 39) keeps its cap — it sits in the 50/50 hero column which is already ~528px wide max at lg+, so the cap is functionally a no-op there.

## Verification

- `npm run build`: 7 pages built
- Visual: body text in "How I work" and "Beyond work" extends to fill the right 3 columns of the grid (≈75% of 1120px container ≈ 840px wide on desktop)
