---
phase: quick
plan: 260506-zm4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/[id].astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "Header + hero image use max-w-[1200px] mx-auto px-[var(--spacing-lg)] (matching reference)"
    - "Body article uses max-w-[720px] mx-auto px-[var(--spacing-lg)] pb-[var(--spacing-4xl)]"
    - "Back link is in its own container above the header (separated, not adjacent)"
    - "Back link uses arrow icon + text (not just text)"
    - "H1 is serif, clamp(2.5rem,5vw,4.5rem), max-w-[20ch], leading-[1.05]"
    - "Summary uses text-xl md:text-2xl with max-w-[55ch] and leading-relaxed"
    - "Eyebrow uses text-xs tracking-widest uppercase (replacing prior text-[13px] tracking-[0.08em])"
    - "Timeline / Team / My Role render as 3-col grid on desktop, single column on mobile, with NO background card"
    - "Hero image uses rounded-2xl (replacing rounded-lg)"
    - "MDX h2 in body is serif text-3xl mt-16 mb-6 (replacing prior small-uppercase-label style)"
    - "MDX h3 in body is serif text-2xl my-4"
    - "Prev/Next navigation is preserved within max-w-[720px] body article, adapted to new typography"
    - "Existing scroll animation behavior on .case-prose h2 is preserved"
  artifacts:
    - path: "src/pages/projects/[id].astro"
      provides: "Redesigned case study page matching reference grid + typography"
      contains: "max-w-[1200px]"
  key_links:
    - from: "src/pages/projects/[id].astro"
      to: "src/scripts/scroll-animation.ts"
      via: "import directive in inline script"
      pattern: "scroll-animation"
---

<objective>
Redesign the project details page (`src/pages/projects/[id].astro`) to match the grid, layout, and typography of `https://www.tushar.work/design/microsoft-edge`. The new layout uses a 1200px max-width container for header + hero image and a narrower 720px column for body prose. Removes the styled metadata card; replaces small-label H2s with serif H2s in the body. Preserves the Timeline/Team/My Role section (adapted to a label-value grid with no card) and the prev/next navigation (within the 720px article column).

Out of scope: changes to MDX content, Content Collection schema, BaseLayout, or any other page.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

### Task 1: Rewrite [id].astro to match reference grid + typography

**Files:** `src/pages/projects/[id].astro`

**Action:**
Replace the existing single-column-1440-wide layout with a multi-tier structure:

1. **Back link** in its own `max-w-[1200px]` container with `pt-[var(--spacing-lg)]`, using an SVG arrow + "Back to work" text.
2. **Header** in `max-w-[1200px]` with `pt-[var(--spacing-xl)] pb-[var(--spacing-xl)]`:
   - eyebrow paragraph with role + skills (joined by `, `)
   - serif H1 with clamp font-size, max-w-[20ch], leading-[1.05]
   - summary paragraph (text-xl md:text-2xl, max-w-[55ch], leading-relaxed)
   - Timeline / Team / My Role 3-col grid (no card / bg-secondary), each with text-xs uppercase label + text-sm value
3. **Hero image** in `max-w-[1200px]` with `mb-[var(--spacing-2xl)]`, using `rounded-2xl` (was `rounded-lg`).
4. **Body article** in `max-w-[720px]` with `pb-[var(--spacing-4xl)]`:
   - MDX `<Content>` rendering with `FullBleedImage` available
   - prev/next nav (preserved) at end, with new text-xs uppercase label + text-sm title styling
5. **Style block** (`<style is:global>`):
   - `.case-prose h2`: serif, 1.875rem, mt-3xl mb-lg, color-text-primary (replaces small-uppercase-label style)
   - `.case-prose h3`: NEW — serif, 1.5rem, my-md, color-text-primary
   - `.case-prose p`: 1rem, line-height 1.625, color-text-secondary, my-md
   - Animate-on-scroll behavior on h2 preserved (opacity/transform with `--stagger-index`)
   - `prefers-reduced-motion` honored

Container choice: inline `max-w-[1200px] mx-auto px-[var(--spacing-lg)]` per section (matching reference's pattern). No new global CSS class.

**Verify:**
- `grep -n "max-w-\[1200px\]" src/pages/projects/\[id\].astro` returns 3 matches (back link + header + hero image).
- `grep -n "max-w-\[720px\]" src/pages/projects/\[id\].astro` returns 1 match (body article).
- `grep -n "rounded-2xl" src/pages/projects/\[id\].astro` returns 1 match (hero image wrapper).
- `grep -n "bg-\[var(--color-secondary)\]" src/pages/projects/\[id\].astro` returns 1 match (image bg, NOT a metadata card).
- `grep -n "Timeline\|Team\|My Role" src/pages/projects/\[id\].astro` returns 3 matches (one per dt).

**Done when:**
- Page builds without TS errors (`astro check` clean for the file).
- Visual check: layout mirrors reference — narrow back link area, wide header/image, narrow body, stacked metadata under summary.
- Prev/next still works (wrap-around behavior preserved from getStaticPaths).
- Scroll animation on body H2s still triggers.

</tasks>
