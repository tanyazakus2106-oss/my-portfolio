---
quick_id: 260706-k07
title: Change light-mode background to #f7f6f7 (soft neutral grey)
status: complete
date: 2026-07-06
commit: 2035c19
---

# Quick Task 260706-k07 — Summary

## What changed

`src/styles/global.css`, +2 / -2 — light-mode `--color-background` #fcfcfc → #f7f6f7
in both places:
- `@theme` default light (line 34)
- `:root.light` manual override (line 72)

Dark mode (#141414) and all other tokens untouched.

## Note

Value was iterated mid-task before any commit: #fcfcfc (baseline) → #f9f6f0
(warm) → #fbf9ff (cool) → **#f7f6f7 (soft neutral grey)**. Task dir renamed to
match each time. The committed diff is a single #fcfcfc → #f7f6f7 change.

## Effect

- Light-mode page background is now soft neutral light grey #f7f6f7 (slightly
  darker/greyer than the original bright #fcfcfc), applied in both the OS-default
  and manual-toggle definitions.
- Near-neutral tone (R=B=247, G=246) — no warm/cool bias, so it sits fine with
  the existing greys; no coherence follow-up needed.
- No hardcoded background copies elsewhere (body uses `bg-[var(--color-background)]`),
  so this is the complete change.

## Verification

- `npm run build`: **OK**. dist CSS has `--color-background:#f7f6f7`; no #fcfcfc /
  #f9f6f0 / #fbf9ff remain in src, public, or dist.
- `npm run typecheck`: **0 errors**.
- `prettier` on changed file (unchanged).
- dev server localhost:4321 (HMR) reflects it live.

## Not committed

Staged, awaiting owner approval.
