---
quick_id: 260615-12x
status: complete
date: 2026-06-14
---

# Quick Task 260615-12x: Scroll reveal trigger 12% → 24% — Summary

## What changed

`src/scripts/scroll-animation.ts` — IntersectionObserver `rootMargin` bottom
changed from `-12%` to `-24%`. Each `.animate-on-scroll` target now waits until
it is ~24% up into the viewport before revealing (a later, more deliberate
trigger). Comment updated to match.

## Verification

- `npm run build` → Complete; confirmed `rootMargin:"0px 0px -24% 0px"` in the
  bundled `dist/_astro/*.js`.

## Notes

- Visual confirmation pending in browser (sandbox blocks dev-server port bind;
  no headless browser installed). `npm run dev` + hard-refresh to feel the timing.
