---
phase: quick
plan: 260506-zm2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Dark mode --color-dominant equals #141414 in all three locations (light default replaced + @media dark + .dark class)"
    - "Light mode --color-dominant equals #fcfcfc in both locations (root default + .light class)"
    - "body::after rule is removed from global.css"
    - "Particles, scroll animations, hero word entrance, and all other styles are unchanged"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Updated color tokens, no noise overlay pseudo-element"
      contains: "#141414"
  key_links:
    - from: "body element"
      to: "var(--color-dominant)"
      via: "Tailwind utility bg-[var(--color-dominant)] in BaseLayout"
      pattern: "color-dominant"
---

<objective>
Update the dark and light mode dominant background colors and remove the global noise texture overlay. Keep the particles network and all other styling untouched.

Color changes:
- Dark mode: `#0F0F0F` → `#141414`
- Light mode: `#FFFFFF` → `#fcfcfc`

Remove `body::after { ... }` block (the noise overlay that produced visible tile-seam banding under browser zoom). Also remove `body { position: relative }` declaration added in the previous task — it was load-bearing only for the noise pseudo-element and is now unused.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

### Task 1: Update color tokens and remove noise overlay

**Files:** `src/styles/global.css`

**Action:**
1. In the default `@theme { ... }` block (light mode tokens): change `--color-dominant: #FFFFFF` → `--color-dominant: #fcfcfc`.
2. In the `@media (prefers-color-scheme: dark) { :root { ... } }` block: change `--color-dominant: #0F0F0F` → `--color-dominant: #141414`.
3. In the `:root.dark { ... }` block: change `--color-dominant: #0F0F0F` → `--color-dominant: #141414`.
4. In the `:root.light { ... }` block: change `--color-dominant: #FFFFFF` → `--color-dominant: #fcfcfc`.
5. Remove the entire `body::after { ... }` rule (and the `/* ─── Global noise texture overlay ─── */` comment + the one-line context comment).
6. Remove the `position: relative;` line from the `body { ... }` rule (now unused — was anchoring the absolute pseudo-element).

**Verify:**
- `grep -n "#0F0F0F\|#FFFFFF" src/styles/global.css` returns no matches for the dominant color.
- `grep -n "#141414" src/styles/global.css` shows three occurrences.
- `grep -n "#fcfcfc" src/styles/global.css` shows two occurrences.
- `grep -n "body::after" src/styles/global.css` returns no matches.
- `grep -n "position: relative" src/styles/global.css` returns no matches inside the `body` rule.

**Done when:**
- Dark mode renders at `#141414` everywhere — no zoom-induced banding visible.
- Light mode renders at `#fcfcfc`.
- Particles still render in the hero (unchanged).

</tasks>
