---
phase: quick
plan: 260506-zm3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/MobileNav.astro
  - src/components/Header.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "--color-background is defined in all four token blocks (light @theme + @media dark + .dark + .light) with the values previously held by --color-dominant"
    - "All references to --color-dominant in src/ resolve to --color-background"
    - "Header bg uses var(--color-background) inside the color-mix"
    - "MobileNav overlay bg uses var(--color-background)"
    - "BaseLayout body uses bg-[var(--color-background)]"
    - "No reference to --color-dominant remains in src/"
  artifacts:
    - path: "src/styles/global.css"
      provides: "--color-background token in all four token blocks"
      contains: "--color-background"
  key_links:
    - from: "--color-background tokens in global.css"
      to: "BaseLayout, Header, MobileNav"
      via: "Tailwind arbitrary value bg-[var(--color-background)] / CSS var() in <style>"
      pattern: "var(--color-background)"
---

<objective>
Rename the `--color-dominant` design token to `--color-background` across the live source. The previous name was idiosyncratic; `--color-background` is the standard, semantically clear name for a page-bg token. Token values stay exactly the same — this is purely a rename refactor.

Out of scope: historical planning docs in `.planning/phases/` reference `--color-dominant` as a record of what was built at that point in time; those are not modified.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

### Task 1: Rename --color-dominant → --color-background across all source files

**Files:**
- `src/styles/global.css` (4 token definitions)
- `src/components/MobileNav.astro` (1 reference)
- `src/components/Header.astro` (1 reference)
- `src/layouts/BaseLayout.astro` (1 reference)

**Action:**
Single-token rename. In each file, replace `--color-dominant` with `--color-background`. No value changes. No structural changes.

**Verify:**
- `grep -rn "color-dominant" src/` returns no matches.
- `grep -rn "color-background" src/` returns 7 matches (4 in global.css + 3 references).

**Done when:**
- Build passes (`npm run build` or `astro check`).
- Visual check: pages render identically — no color regressions.

</tasks>
