---
phase: quick-260615-2dz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/about.astro]
autonomous: true
requirements: [QUICK-2dz]

must_haves:
  truths:
    - "About page hero header scrolls normally with the page at all viewports (no sticky behavior)"
    - "No lg:sticky or lg:top-* utility remains on the about hero header"
    - "Code comments no longer claim sticky behavior that no longer exists"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "About page hero header without sticky positioning"
      contains: "order-1 lg:order-2"
  key_links: []
---

<objective>
Remove sticky positioning from the About page hero header so it scrolls normally with the page at all viewports.

Purpose: The sticky text column on lg+ is being removed in favor of a normal-flow scroll. The hero header should move with the page like every other element.
Output: Updated `src/pages/about.astro` — two sticky utility classes removed from the `<header>` and two stale comments corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<interfaces>
<!-- Exact current state of the three lines to change in src/pages/about.astro -->

Line 15 (grid-container comment — ends with stale sticky claim):
```
/* Hero grid — mirrors tushar.work/about. lg+ = 50/50 image-left / text-right; below lg = single column with text-above-image (Tailwind order-* visual reorder, DOM source order preserved). Text column sticky on lg+. */
```

Line 37 (header comment — ends with stale sticky claim):
```
/* Block 2 — Eyebrow + hero headline + intro. Visual order 1 below lg (lede first on mobile), order 2 at lg+ (sits right of image). Sticky on lg+ at top-128px (header is 80px sticky, leaves 48px buffer). */
```

Line 40 (header class string — the two sticky utilities to remove):
```
class="order-1 lg:order-2 lg:sticky lg:top-[calc(var(--spacing-4xl)+var(--spacing-xl))]"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove sticky utilities from about hero header and fix stale comments</name>
  <files>src/pages/about.astro</files>
  <action>
Make exactly three surgical edits in `src/pages/about.astro`. Do NOT add styles, change layout, or touch anything else.

1. On the `<header>` class string (around line 40), remove the two sticky-related utility classes — `lg:sticky` and `lg:top-[calc(var(--spacing-4xl)+var(--spacing-xl))]`. The class string must become exactly:
   `class="order-1 lg:order-2"`

2. On the grid-container comment (around line 15), remove the trailing sentence " Text column sticky on lg+." so the comment no longer claims sticky behavior. The comment should now end at "...DOM source order preserved)." and close with `*/`.

3. On the header comment (around line 37), remove the trailing sentence " Sticky on lg+ at top-128px (header is 80px sticky, leaves 48px buffer)." so the comment ends at "...(sits right of image)." and closes with `*/`.

After editing, run `npm run format` so Prettier reflows the touched lines cleanly, then `npm run typecheck`.
  </action>
  <verify>
    <automated>! grep -q "lg:sticky" src/pages/about.astro && ! grep -qi "sticky" src/pages/about.astro && npm run typecheck</automated>
  </verify>
  <done>The about hero `<header>` class is exactly `order-1 lg:order-2`; no `lg:sticky` or `lg:top-*` utility remains; neither stale comment mentions sticky; `grep -i sticky src/pages/about.astro` returns nothing; `npm run typecheck` passes; `npm run format` leaves the file clean.</done>
</task>

</tasks>

<verification>
- `grep -i "sticky" src/pages/about.astro` returns no matches
- `npm run typecheck` (astro check) passes with no new errors
- `npm run format` leaves `src/pages/about.astro` unchanged after the edit (already formatted)
- Header class string is exactly `order-1 lg:order-2`
</verification>

<success_criteria>
- About hero header scrolls normally with the page at all viewports (sticky removed)
- No sticky utility classes or stale sticky comments remain in `src/pages/about.astro`
- No other layout, styles, or content changed
- typecheck and format both clean
</success_criteria>

<output>
After completion, create `.planning/quick/260615-2dz-remove-sticky-positioning-from-about-pag/260615-2dz-SUMMARY.md`
</output>
