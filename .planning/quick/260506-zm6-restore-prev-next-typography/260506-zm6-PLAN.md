---
phase: quick
plan: 260506-zm6
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/[id].astro
autonomous: true
requirements: []

must_haves:
  truths:
    - "Prev/next labels use text-[13px] uppercase tracking-[0.08em] (pre-zm4 sizing)"
    - "Prev/next titles use text-base (pre-zm4 sizing)"
    - "Hover states from zm5 (sweep-underline, accent color) preserved"
    - "Layout placement (inside max-w-[720px] body article) preserved"
  artifacts:
    - path: "src/pages/projects/[id].astro"
      provides: "Pre-zm4 typography on prev/next nav, with zm5 hover states intact"
      contains: "text-[13px] uppercase tracking-[0.08em]"
  key_links: []
---

<objective>
Restore the prev/next case-study nav typography to its pre-zm4 state. zm4 swapped the label sizing to `text-xs tracking-widest` and the title to `text-sm` to match the reference page; user prefers the older, slightly larger sizing for this specific component. Hover states from zm5 and structural layout from zm4 stay as-is.
</objective>

<context>
@.planning/STATE.md
@.planning/quick/260506-zm5-restore-prev-next-hover-states/260506-zm5-SUMMARY.md
</context>

<tasks>

### Task 1: Revert prev/next typography to pre-zm4 sizes

**Files:** `src/pages/projects/[id].astro`

**Action:**
1. Label spans: `class="text-xs tracking-widest uppercase"` → `class="text-[13px] uppercase tracking-[0.08em]"` (both prev and next).
2. Title spans: `text-sm text-[var(--color-text-primary)]` → `text-base text-[var(--color-text-primary)]` (both prev and next).

No other changes — the inner `<span class="relative after:...">` underline-sweep wrappers and the anchor-level `hover:text-[var(--color-accent)]` stay untouched.

**Verify:**
- `grep -c "text-\[13px\]" src/pages/projects/\[id\].astro` returns 2.
- `grep -c "tracking-\[0.08em\]" src/pages/projects/\[id\].astro` returns 2.
- `grep "text-sm text-\[var(--color-text-primary)\]" src/pages/projects/\[id\].astro` returns no matches inside the nav element.
- `grep -c "group-hover:after:scale-x-100" src/pages/projects/\[id\].astro` still returns 2 (zm5 hover states intact).

**Done when:**
- Labels look slightly larger and use the more relaxed 0.08em tracking.
- Titles render at 16px (text-base) again.
- Hover still triggers accent color + sweep underline.

</tasks>
