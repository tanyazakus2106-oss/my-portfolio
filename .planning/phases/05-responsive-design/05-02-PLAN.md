---
phase: 05-responsive-design
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/Footer.astro
  - src/components/FullBleedImage.astro
  - .planning/phases/05-responsive-design/05-AUDIT.md
autonomous: true
requirements:
  - RESP-01
  - RESP-03
tags:
  - responsive
  - touch-targets
  - overflow

must_haves:
  truths:
    - "The footer social-icon button tap area is at least 44×44 px (no longer 40×40)"
    - "Rendering a page that contains a FullBleedImage figure produces no horizontal overflow on any browser (scrollbar-visible browsers included)"
    - "The Footer focus-visible outline pattern is preserved (no regression in keyboard focus styles)"
    - "Existing transitions, colors, and border tokens on `.footer-icon-btn` are preserved — only width/height change"
  artifacts:
    - path: "src/components/Footer.astro"
      provides: "Footer with WCAG-2.5.5-compliant 44px social icon tap targets"
      contains: "width: 44px"
    - path: "src/components/FullBleedImage.astro"
      provides: "FullBleedImage whose `100vw` breakout no longer triggers horizontal page overflow"
      contains: "overflow-x: hidden"
  key_links:
    - from: "src/components/FullBleedImage.astro `.full-bleed` figure scoped CSS"
      to: "Horizontal-overflow audit row in 05-AUDIT.md (`FullBleedImage (in case prose)` row, all 4 columns)"
      via: "self-contained overflow-x: hidden on the breakout container"
      pattern: "\\.full-bleed[^{]*\\{[^}]*overflow-x:\\s*hidden"
    - from: "src/components/Footer.astro `.footer-icon-btn` scoped CSS"
      to: "`Footer (grid reflow, icon-btn tap targets)` audit row in 05-AUDIT.md"
      via: "44px width/height satisfies D-04 minimum"
      pattern: "footer-icon-btn[^}]*width:\\s*44px"
---

<objective>
Resolve the two pre-identified pre-audit failures recorded in `05-AUDIT.md` Pre-Identified Failures table:

1. `Footer.astro` `.footer-icon-btn` is `40×40 px` — 4 px short of the D-04 touch-target minimum (RESP-03).
2. `FullBleedImage.astro` uses a `100vw` negative-margin breakout and references a non-existent `<body> overflow-x: hidden` guard in BaseLayout; on browsers with visible scrollbars this produces horizontal page overflow (RESP-01).

Both fixes are surgical, scoped to single scoped-CSS blocks, and require zero JS or Tailwind utility changes. They are independent of the rest of the audit and can land in Wave 2 in parallel with the audit-execution plan (05-06) and the other Wave-2 fix plans, because each modifies a different source file.

Output: Updated `Footer.astro` (44px tap target) and `FullBleedImage.astro` (self-contained overflow guard), plus audit-matrix updates marking the two pre-identified-failure rows as resolved with the commit SHA.

Implements: per CONTEXT.md D-04 (44px on `footer-icon-btn`), per CONTEXT.md D-06 (FullBleedImage authoring simplicity preserved — Path A from RESEARCH §2 Surface 3), per CONTEXT.md D-01 escape hatch (single-offender overflow → fix the offender, do not blanket `<body>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-responsive-design/05-CONTEXT.md
@.planning/phases/05-responsive-design/05-RESEARCH.md
@.planning/phases/05-responsive-design/05-PATTERNS.md
@.planning/phases/05-responsive-design/05-AUDIT.md
@src/components/Footer.astro
@src/components/FullBleedImage.astro
@src/styles/global.css
@CLAUDE.md

<interfaces>
<!-- Current state of the two target files, extracted verbatim from PATTERNS.md and RESEARCH.md. -->

From src/components/Footer.astro (current — confirmed in PATTERNS.md):
```css
/* lines 88-108 (scoped <style> block) */
.footer-icon-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 40px;   /* FAIL — 4px below D-04 minimum */
  height: 40px;  /* FAIL — 4px below D-04 minimum */
  border-radius: 9999px;
  color: var(--color-text-primary);
  background-color: transparent;
  border: 1px solid color-mix(in srgb, var(--color-border) 80%, transparent);
  transition: border-color 150ms ease, color 150ms ease;
}
.footer-icon-btn:hover {
  /* hover state — preserve unchanged */
}
.footer-icon-btn:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
```

From src/components/FullBleedImage.astro (current — confirmed in PATTERNS.md):
```html
<!-- line 21 (the <img> tag — DO NOT modify in this plan; image-pipeline work is 05-05) -->
<img src={src} alt={alt} loading="lazy" decoding="async" />

<!-- lines 24-35 (scoped <style> block — the overflow fix lands here) -->
<style>
  .full-bleed {
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    margin-right: calc(-50vw + 50%);
  }
  .full-bleed img {
    width: 100%;
    height: auto;
    display: block;
  }
</style>
```

Analog reference (Header.astro — `.tz-logo` already at 44px):
```css
.tz-logo {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  ...
}
```

CLAUDE.md token reminder: design tokens like `var(--color-border)`, `var(--color-accent)` MUST be preserved verbatim. No raw hex.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump `.footer-icon-btn` to 44×44 px in Footer.astro</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/components/Footer.astro (read the entire scoped `<style>` block at lines 87–108 to see all declarations — your edit must preserve every other rule unchanged)
    - .planning/phases/05-responsive-design/05-PATTERNS.md (§ src/components/Footer.astro — confirms the analog fix matches Header.astro `.tz-logo` 44px pattern)
    - src/components/Header.astro (lines 87–140 — the analog 44px scoped-CSS pattern, especially `width: 44px; height: 44px;` paired with `display: flex; align-items: center;`)
  </read_first>
  <action>
In `src/components/Footer.astro`, inside the scoped `<style>` block, locate the `.footer-icon-btn` rule. Change exactly two declarations:

```diff
 .footer-icon-btn {
   display: inline-flex;
   align-items: center;
   justify-content: center;
-  width: 40px;
-  height: 40px;
+  width: 44px;
+  height: 44px;
   border-radius: 9999px;
   color: var(--color-text-primary);
   background-color: transparent;
   border: 1px solid color-mix(in srgb, var(--color-border) 80%, transparent);
   transition: border-color 150ms ease, color 150ms ease;
 }
```

**Preserve verbatim** (do NOT modify):
- `display: inline-flex;` and the two `align-items` / `justify-content` lines (required to make `width`/`height` apply per RESEARCH §3 Pitfall 5)
- `border-radius: 9999px` (full circle)
- The `color`, `background-color`, `border` declarations (design tokens — CLAUDE.md rule)
- The `transition` line
- The `.footer-icon-btn:hover` rule block entirely
- The `.footer-icon-btn:focus-visible` rule block entirely (`outline: 2px solid var(--color-accent); outline-offset: 2px;`)
- All Tailwind utility classes on the `<a>` elements in the JSX above the `<style>` block

Do not touch the JSX template, the imports, or any other CSS rule in the file. This is a 2-line edit.

Rationale per RESEARCH §3 Pre-identified Issue: A1 records the visual change is imperceptible at the border-circle scale (4 px increase on a 40px circle). Option A from RESEARCH §3.
  </action>
  <verify>
    <automated>grep -E "^\s*width:\s*44px;" src/components/Footer.astro && grep -E "^\s*height:\s*44px;" src/components/Footer.astro && ! grep -E "footer-icon-btn[\s\S]*40px" src/components/Footer.astro && npm run typecheck 2>&1 | tee /tmp/05-02-typecheck.log | tail -5 && grep -q "0 errors" /tmp/05-02-typecheck.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'width:\s*44px' src/components/Footer.astro` returns a line inside the `.footer-icon-btn` rule (verify by `grep -B1 -A4 'footer-icon-btn {' src/components/Footer.astro` showing the new value)
    - `grep -nE 'height:\s*44px' src/components/Footer.astro` returns a matching line for height
    - No occurrence of `40px` remains in the `.footer-icon-btn` rule: `grep -A 12 '.footer-icon-btn {' src/components/Footer.astro | grep -c '40px'` returns 0
    - Focus-visible rule still present: `grep -F '.footer-icon-btn:focus-visible' src/components/Footer.astro` returns a match and the following lines still contain `outline: 2px solid var(--color-accent);`
    - Transition rule preserved: `grep -F 'transition: border-color 150ms ease, color 150ms ease;' src/components/Footer.astro` returns a match
    - Design tokens preserved: `grep -F 'var(--color-border)' src/components/Footer.astro && grep -F 'var(--color-accent)' src/components/Footer.astro` both return matches
    - Type check passes: `npm run typecheck` exits 0 with no new errors (Astro check has no opinion on scoped CSS but verifies no template damage)
    - Production build runs clean: `npm run build` exits 0
    - DevTools console snippet (run manually after `npm run preview` at 375px on the home page): the touch-target snippet from `05-AUDIT.md` Methodology returns NO entry whose `class` includes `footer-icon-btn` and `w < 44` or `h < 44`
  </acceptance_criteria>
  <done>
`.footer-icon-btn` is `44px × 44px`. All sibling declarations, hover state, and focus-visible state are byte-identical to pre-edit. `npm run build` and `npm run typecheck` both pass. The `Footer (grid reflow, icon-btn tap targets)` audit row in `05-AUDIT.md` is marked PASS at all four breakpoints with the commit SHA cited (Task 3 handles the audit-matrix write).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add self-contained overflow guard to FullBleedImage `.full-bleed` figure</name>
  <files>src/components/FullBleedImage.astro</files>
  <read_first>
    - src/components/FullBleedImage.astro (read the entire file — verify the scoped `<style>` block is at lines 24–35 as PATTERNS.md states, and that the comment on line 5 references a `<body> overflow-x:hidden` guard you are intentionally NOT adding)
    - .planning/phases/05-responsive-design/05-PATTERNS.md (§ src/layouts/BaseLayout.astro — confirms the planner's targeted-fix preference per D-01 escape hatch: fix the offender, not the body)
    - .planning/phases/05-responsive-design/05-RESEARCH.md (§5 Known Horizontal Overflow Risk — confirms this is the single offender; §7 Pitfall 1 — describes the missing guard)
    - src/layouts/BaseLayout.astro (read to confirm there is no existing `overflow-x` declaration on `<body>` — protects against accidentally adding a redundant rule)
  </read_first>
  <action>
In `src/components/FullBleedImage.astro`, modify ONLY the scoped `<style>` block (lines 24–35). Add one declaration to the `.full-bleed` rule:

```diff
 <style>
   .full-bleed {
     width: 100vw;
     margin-left: calc(-50vw + 50%);
     margin-right: calc(-50vw + 50%);
+    overflow-x: hidden;
   }
   .full-bleed img {
     width: 100%;
     height: auto;
     display: block;
   }
 </style>
```

Also update the stale comment in the component's JSX/frontmatter area (line 5 region per PATTERNS.md) that claims `overflow-x:hidden` exists on BaseLayout `<body>`. Replace that comment with an accurate description. Example replacement:

```diff
- /* The 100vw breakout relies on <body> overflow-x:hidden from BaseLayout to prevent horizontal scrollbars. */
+ /* The 100vw breakout uses an `overflow-x: hidden` on the .full-bleed figure itself to prevent horizontal scrollbars without imposing a global body-level guard (per D-01 escape hatch: single offender → targeted fix). */
```

If the exact comment text differs, edit whatever comment refers to the non-existent BaseLayout guard so it matches the new reality.

**Preserve verbatim**:
- The `<img>` tag and all its attributes (image-pipeline work is plan 05-05, not this plan)
- The Astro frontmatter and `Props` interface
- The `<figure class="full-bleed my-[var(--spacing-2xl)]">` wrapper
- The `.full-bleed img` rule block entirely
- Every other character of the file outside the two diffs above

Do NOT modify `src/layouts/BaseLayout.astro`. The escape hatch in CONTEXT.md D-01 says blanket body-level guards trigger only when 3+ offenders share the same root cause; only FullBleedImage is a confirmed offender, so the targeted scoped-CSS fix wins.

Rationale: per PATTERNS.md "Alternative (preferred if only FullBleedImage causes overflow): Add `overflow-x: hidden` to the `.full-bleed` figure container in `FullBleedImage.astro`'s scoped style block instead of blanketing the entire `<body>`."
  </action>
  <verify>
    <automated>grep -E "^\s*overflow-x:\s*hidden;" src/components/FullBleedImage.astro && ! grep -E "overflow-x" src/layouts/BaseLayout.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `overflow-x: hidden` lives inside the `.full-bleed` rule: `grep -A 5 '.full-bleed {' src/components/FullBleedImage.astro | grep -q 'overflow-x: hidden'`
    - BaseLayout is UNCHANGED: `grep -c 'overflow-x' src/layouts/BaseLayout.astro` returns 0
    - Negative-margin breakout preserved: `grep -F 'margin-left: calc(-50vw + 50%);' src/components/FullBleedImage.astro` returns a match
    - `<img>` tag is untouched: `grep -F '<img src={src} alt={alt} loading="lazy" decoding="async"' src/components/FullBleedImage.astro` returns a match (Path A image-pipeline work is in 05-05)
    - Stale comment about BaseLayout overflow-x guard is no longer present: `grep -c 'overflow-x:hidden from BaseLayout' src/components/FullBleedImage.astro` returns 0
    - Production build succeeds: `npm run build` exits 0
    - Type check passes: `npm run typecheck` exits 0
    - DevTools overflow snippet (run manually at 375 px on a case study page with a `<FullBleedImage>` in MDX, after `npm run preview`): the snippet from `05-AUDIT.md` returns an empty offenders table AND `document.documentElement.scrollWidth > document.documentElement.clientWidth` returns `false`
  </acceptance_criteria>
  <done>
`.full-bleed` figure carries its own `overflow-x: hidden`. BaseLayout is untouched. The component comment accurately describes the guard. Build and typecheck pass. The DevTools overflow snippet returns `false` on `/projects/[id]` pages at all 4 breakpoints. The `FullBleedImage (in case prose)` audit row in `05-AUDIT.md` can be marked PASS at all 4 breakpoints (Task 3 handles audit-matrix write).
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark the two pre-identified-failure audit rows resolved in 05-AUDIT.md</name>
  <files>.planning/phases/05-responsive-design/05-AUDIT.md</files>
  <read_first>
    - .planning/phases/05-responsive-design/05-AUDIT.md (verify the Pre-Identified Failures table from 05-01-PLAN exists; locate the two rows for `.footer-icon-btn` and `FullBleedImage.astro figure`)
    - .planning/phases/05-responsive-design/05-RESEARCH.md (§8 — confirms the audit-row update is the validation artifact for these fixes)
  </read_first>
  <action>
After Tasks 1 and 2 commits land, capture both commit SHAs (`git log --oneline -2`) and update `.planning/phases/05-responsive-design/05-AUDIT.md`:

1. In the **Audit Matrix** table, fill the cells for two rows:
   - `Footer (grid reflow, icon-btn tap targets)` row — set all four breakpoint columns (375, 768, 1024, 1440) to `PASS`. Add to Notes: `tap target 40→44 (commit <SHA-task-1>)`.
   - `FullBleedImage (in case prose)` row — set all four breakpoint columns to `PASS`. Add to Notes: `overflow-x guard on .full-bleed (commit <SHA-task-2>)`.

2. In the **Pre-Identified Failures** table, add a new column or append to the existing row text to indicate the resolution. Append ` — RESOLVED in <SHA>` to each of the two rows that this plan addresses (the `footer-icon-btn` row and the `FullBleedImage.astro figure` row).

3. Do NOT modify any other matrix rows — those are filled by 05-03 (MobileNav), 05-04 / 05-05 (image plans), and 05-06 (full audit execution).

Use the literal commit SHAs from `git log --oneline -2 src/components/Footer.astro src/components/FullBleedImage.astro` or whatever git history reveals. If both fixes ship in one combined commit (acceptable for this plan), use that single SHA for both rows.
  </action>
  <verify>
    <automated>grep -E "Footer \(grid reflow.*PASS.*PASS.*PASS.*PASS" .planning/phases/05-responsive-design/05-AUDIT.md && grep -E "FullBleedImage \(in case prose\).*PASS.*PASS.*PASS.*PASS" .planning/phases/05-responsive-design/05-AUDIT.md && grep -F "RESOLVED" .planning/phases/05-responsive-design/05-AUDIT.md | wc -l | awk '$1 >= 2 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Footer row updated: `grep 'Footer (grid reflow' .planning/phases/05-responsive-design/05-AUDIT.md` shows a line where the 375 / 768 / 1024 / 1440 cells all contain `PASS`
    - FullBleedImage row updated: `grep 'FullBleedImage (in case prose)' .planning/phases/05-responsive-design/05-AUDIT.md` shows a line where the 375 / 768 / 1024 / 1440 cells all contain `PASS`
    - Both pre-identified-failure rows annotated with `RESOLVED`: `grep -c RESOLVED .planning/phases/05-responsive-design/05-AUDIT.md` returns ≥ 2
    - Other 13 matrix rows are UNCHANGED (still empty cells): `grep 'MobileNav overlay' .planning/phases/05-responsive-design/05-AUDIT.md | grep -qv PASS` — i.e. the MobileNav overlay row does not yet contain PASS (that's plan 05-03's job)
    - Commit SHA present in Notes column: `grep -E '[0-9a-f]{7}' .planning/phases/05-responsive-design/05-AUDIT.md` returns at least one match (a short SHA)
  </acceptance_criteria>
  <done>
The audit matrix records both pre-identified failures as PASS in all four breakpoint columns with commit SHAs cited. The Pre-Identified Failures table marks the same two issues `RESOLVED`. No other matrix cells were touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport boundary | `100vw` breakout in FullBleedImage interacts with browser scrollbar rendering; cross-browser variation in scrollbar width affects overflow behavior. |
| MDX → component prop boundary | `FullBleedImage` accepts a `src: string` prop from MDX content. In this plan we do NOT change the prop interface (image-pipeline migration is plan 05-05), so the trust boundary is unchanged from current state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | I (Information disclosure) | `FullBleedImage` `src` attribute | accept | `src` is a `public/` path string controlled by Tanya (the sole author). No user-supplied input. MDX files are authored under git review. No XSS surface introduced by this plan (we add CSS only). |
| T-05-02-02 | T (Tampering) | `.footer-icon-btn` CSS | accept | CSS is build-time static. No runtime tampering surface. |
| T-05-02-03 | D (Denial of service) | `overflow-x: hidden` on `.full-bleed` figure | mitigate | Adding `overflow-x: hidden` on a scoped figure could in theory clip intentional content. Mitigation: the `.full-bleed` element's children are limited to a single `<img>` whose CSS is `width: 100%` — no content can horizontally exceed the figure box. Verify via DevTools at all 4 breakpoints during plan 05-06 audit execution. (Assumption A4 in RESEARCH.) |

Severity: all low. No high-severity threats. `security_enforcement` gate passes.
</threat_model>

<verification>
- After Task 1: `grep -E 'width:\s*44px' src/components/Footer.astro && grep -E 'height:\s*44px' src/components/Footer.astro`.
- After Task 2: `grep -A 5 '.full-bleed {' src/components/FullBleedImage.astro | grep 'overflow-x: hidden'`; BaseLayout untouched.
- After Task 3: both target rows in `05-AUDIT.md` show PASS at all 4 columns; Pre-Identified Failures table marks them RESOLVED with commit SHAs.
- `npm run build && npm run typecheck` both pass.
- Manual DevTools verification: at 375 px on `/projects/project-alpha`, the overflow snippet from `05-AUDIT.md` returns no offenders; the touch-target snippet returns no failures whose class contains `footer-icon-btn`.
</verification>

<success_criteria>
- `.footer-icon-btn` is 44×44 px with all sibling declarations preserved.
- `.full-bleed` figure carries its own `overflow-x: hidden`; BaseLayout `<body>` is unchanged.
- Both audit-matrix rows are PASS across all 4 breakpoints with commit SHAs in Notes.
- Build, typecheck pass cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-design/05-02-SUMMARY.md` summarizing: files touched, the two pre-identified failures resolved, audit-matrix rows updated, build status.
</output>
