---
phase: 05-responsive-design
plan: 05
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/components/CaseImage.astro
  - src/components/FullBleedImage.astro
  - src/pages/projects/[id].astro
  - src/content/projects/project-alpha.mdx
  - .planning/phases/05-responsive-design/05-AUDIT.md
autonomous: true
requirements:
  - RESP-02
  - CASE-06
tags:
  - responsive
  - images
  - mdx
  - astro-image
  - component

must_haves:
  truths:
    - "A new `CaseImage` Astro component exists at `src/components/CaseImage.astro` and renders an Astro `<Image>` with `widths={[400, 720, 1440]}` and `sizes=\"(min-width: 768px) 720px, calc(100vw - 3rem)\"`"
    - "The case study page (`projects/[id].astro`) registers `CaseImage` alongside `FullBleedImage` via the `<Content components={...}>` prop so MDX files can reference both"
    - "`FullBleedImage.astro` declares `sizes=\"100vw\"` on its `<img>` tag (Path A from RESEARCH section 2 Surface 3 — minimal implementation that preserves `public/` authoring)"
    - "One MDX template file documents the CaseImage usage pattern in a top-of-file comment so Tanya can copy it when she adds real case study body images"
    - "No MDX file rewrites case-study content — only a usage-comment is added to one template file"
  artifacts:
    - path: "src/components/CaseImage.astro"
      provides: "Reusable MDX-injectable wrapper for inline case study body images with built-in responsive widths/sizes"
      exports: "default Astro component with Props { src: ImageMetadata; alt: string; caption?: string }"
    - path: "src/components/FullBleedImage.astro"
      provides: "Full-bleed figure component whose <img> now carries `sizes=\"100vw\"`"
      contains: "sizes=\"100vw\""
    - path: "src/pages/projects/[id].astro"
      provides: "Case study render route that exposes both FullBleedImage and CaseImage to MDX"
      contains: "{ FullBleedImage, CaseImage }"
  key_links:
    - from: "src/content/projects/*.mdx body images (future authoring)"
      to: "<CaseImage src={importedImg} alt=...> usage pattern"
      via: "components prop on <Content components={...}> in [id].astro"
      pattern: "components=\\{\\{[^}]*CaseImage"
    - from: "src/components/FullBleedImage.astro <img>"
      to: "Browser srcset hint sizes=\"100vw\""
      via: "Path A minimal interpretation of D-06"
      pattern: "sizes=\"100vw\""
---

<objective>
Implement the remaining two image surfaces from CONTEXT.md D-06: FullBleedImage and MDX inline images.

For FullBleedImage: take the Path A minimal interpretation from RESEARCH section 2 Surface 3 — add `sizes="100vw"` (and confirm `loading="lazy"`, `decoding="async"`) to the existing `<img>` tag. Do NOT migrate the source assets from `public/` to `src/assets/` (RESEARCH Open Question 1 recommendation: defer migration to a follow-up task; Phase 5 captures the simplest interpretation first). The `100vw` size hint instructs browsers that the image spans the full viewport even without a generated srcset.

For MDX inline images: create a new `CaseImage.astro` wrapper component (RESEARCH section 6 Option A — recommended). Register it in `[id].astro` alongside `FullBleedImage` so MDX files can invoke `<CaseImage src={importedImg} alt="..." />`. The wrapper bakes in `widths={[400, 720, 1440]}` and `sizes="(min-width: 768px) 720px, calc(100vw - 3rem)"` so Tanya never types responsive props when authoring. Add a usage-documentation comment to one MDX template file so the pattern is discoverable without grepping the codebase.

This plan does NOT modify any existing MDX content (no real case study inline images exist yet — verified in RESEARCH section 6 Current Reality Check). The wrapper is a preparedness artifact for when real content arrives.

Implements: RESP-02 (responsive images across all D-06 surfaces), CASE-06 (case study page is correctly laid out on mobile — `sizes="100vw"` on full-bleed images is the canonical responsive hint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-responsive-design/05-CONTEXT.md
@.planning/phases/05-responsive-design/05-RESEARCH.md
@.planning/phases/05-responsive-design/05-PATTERNS.md
@.planning/phases/05-responsive-design/05-AUDIT.md
@src/components/FullBleedImage.astro
@src/pages/projects/[id].astro
@src/content/projects/project-alpha.mdx
@CLAUDE.md

<interfaces>
Current FullBleedImage.astro `<img>` tag (PATTERNS.md line 21):

  <img src={src} alt={alt} loading="lazy" decoding="async" />

Target FullBleedImage.astro `<img>` tag (Path A from PATTERNS.md and RESEARCH section 2 Surface 3):

  <img
    src={src}
    alt={alt}
    loading="lazy"
    decoding="async"
    sizes="100vw"
    class="w-full h-auto block"
  />

Current `<Content components={...}>` in [id].astro (PATTERNS.md line 91 area):

  <Content components={{ FullBleedImage }} />

Target wiring:

  <Content components={{ FullBleedImage, CaseImage }} />

Target CaseImage.astro (PATTERNS.md verbatim template):

  ---
  import { Image } from 'astro:assets';
  interface Props { src: ImageMetadata; alt: string; caption?: string; }
  const { src, alt, caption } = Astro.props;
  ---
  <figure class="my-[var(--spacing-2xl)]">
    <Image
      src={src}
      alt={alt}
      widths={[400, 720, 1440]}
      sizes="(min-width: 768px) 720px, calc(100vw - 3rem)"
      class="w-full h-auto rounded-lg"
    />
    {caption && <figcaption class="mt-[var(--spacing-sm)] text-sm text-[var(--color-text-secondary)]">{caption}</figcaption>}
  </figure>

MDX usage pattern (for the doc-comment, from RESEARCH section 6):

  import CaseImage from '../../components/CaseImage.astro';
  import wireframeImg from './wireframes-v2.jpg';

  <CaseImage src={wireframeImg} alt="Second iteration wireframes" />

Existing MDX FullBleedImage usage in project-alpha.mdx (line 27 — PATTERNS.md):

  <FullBleedImage src="/images/project-alpha-process.jpg" alt="Process overview for Project Alpha — placeholder" />

Comment at top of project-alpha.mdx (lines 13-15 per PATTERNS.md) already documents FullBleedImage. The new CaseImage doc comment lives beside it.

Critical: FullBleedImage was modified in plan 05-02 (added `overflow-x: hidden` to `.full-bleed` scoped CSS). Plan 05-05 modifies the same file but a DIFFERENT region (the `<img>` tag, not the scoped `<style>` block). Plan 05-05 depends on 05-01 only; if 05-02 lands first, the executor must re-read FullBleedImage.astro before editing to see the 05-02 changes. If 05-05 lands before 05-02, the same applies in reverse. Wave 2 plans run in parallel, so the executor must read the current file state in BOTH cases.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/CaseImage.astro</name>
  <files>src/components/CaseImage.astro</files>
  <read_first>
    - src/components/FullBleedImage.astro (the role analog — confirm interface shape, prop naming, and figure-wrapper pattern)
    - src/components/FeaturedCard.astro (Astro `<Image>` widths/sizes/class pattern — already in use in the codebase)
    - .planning/phases/05-responsive-design/05-PATTERNS.md (section src/components/CaseImage.astro — full target template; section Shared Patterns — Astro `<Image>` responsive props pattern)
    - .planning/phases/05-responsive-design/05-RESEARCH.md (section 6 Option A — full rationale and import / usage shape)
    - src/styles/global.css (confirm `--spacing-2xl`, `--spacing-sm`, `--color-text-secondary` tokens exist — they are referenced in the new component class lists)
  </read_first>
  <action>
Create a new file at `src/components/CaseImage.astro` with the following exact content:

  ---
  import { Image } from 'astro:assets';
  interface Props { src: ImageMetadata; alt: string; caption?: string; }
  const { src, alt, caption } = Astro.props;
  ---
  <figure class="my-[var(--spacing-2xl)]">
    <Image
      src={src}
      alt={alt}
      widths={[400, 720, 1440]}
      sizes="(min-width: 768px) 720px, calc(100vw - 3rem)"
      class="w-full h-auto rounded-lg"
    />
    {caption && <figcaption class="mt-[var(--spacing-sm)] text-sm text-[var(--color-text-secondary)]">{caption}</figcaption>}
  </figure>

Constraints:
- Use `ImageMetadata` as the `src` type (Astro-provided global from `astro:assets`)
- The `Image` import path is exactly `astro:assets`
- The class string on `<Image>` is exactly `w-full h-auto rounded-lg`
- The figure margin token is `--spacing-2xl` (48 px in this codebase per `global.css`)
- The caption margin token is `--spacing-sm` (8 px)
- The caption color token is `--color-text-secondary`
- The optional-caption render uses `{caption && <figcaption ...>...</figcaption>}` — not a ternary, not a fragment

Do NOT add:
- A scoped `<style>` block (Tailwind utilities express everything needed per PATTERNS.md)
- Any additional props (the interface is exactly `{ src, alt, caption? }`)
- Any default value for `caption` (the `&&` short-circuit handles `undefined`)
- Any export statement (Astro components are default-exported implicitly via the `.astro` extension)

Verify the file builds before moving to Task 2 — an `astro check` failure here blocks the components-prop wiring in Task 3.
  </action>
  <verify>
    <automated>test -f src/components/CaseImage.astro && grep -F "import { Image } from 'astro:assets'" src/components/CaseImage.astro && grep -F "widths={[400, 720, 1440]}" src/components/CaseImage.astro && grep -F 'sizes="(min-width: 768px) 720px, calc(100vw - 3rem)"' src/components/CaseImage.astro && grep -F "interface Props { src: ImageMetadata; alt: string; caption?: string; }" src/components/CaseImage.astro && npm run typecheck > /tmp/05-05-tc1.log 2>&1 && [ $? = 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/components/CaseImage.astro` exits 0
    - Image import present: `grep -F "import { Image } from 'astro:assets'" src/components/CaseImage.astro` returns a match
    - Props interface exact: `grep -F "interface Props { src: ImageMetadata; alt: string; caption?: string; }" src/components/CaseImage.astro` returns a match
    - widths array exact: `grep -F "widths={[400, 720, 1440]}" src/components/CaseImage.astro` returns a match
    - sizes formula exact: `grep -F 'sizes="(min-width: 768px) 720px, calc(100vw - 3rem)"' src/components/CaseImage.astro` returns a match
    - figure margin token: `grep -F "my-[var(--spacing-2xl)]" src/components/CaseImage.astro` returns a match
    - caption color token: `grep -F "text-[var(--color-text-secondary)]" src/components/CaseImage.astro` returns a match
    - caption short-circuit render: `grep -F "{caption && <figcaption" src/components/CaseImage.astro` returns a match
    - Image class exact: `grep -F 'class="w-full h-auto rounded-lg"' src/components/CaseImage.astro` returns a match
    - No scoped style block: `grep -c "^<style" src/components/CaseImage.astro` returns 0
    - Type check passes: `npm run typecheck` exits 0
    - Build passes: `npm run build` exits 0
  </acceptance_criteria>
  <done>
`src/components/CaseImage.astro` exists with the exact template. Build and typecheck pass. The component is ready to be wired into `[id].astro` (Task 3) and referenced from MDX (Task 4).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sizes=\"100vw\" to the FullBleedImage <img> tag (Path A)</name>
  <files>src/components/FullBleedImage.astro</files>
  <read_first>
    - src/components/FullBleedImage.astro (read the CURRENT state — plan 05-02 may have already added `overflow-x: hidden` to the scoped CSS; this task touches the `<img>` tag, a different region)
    - .planning/phases/05-responsive-design/05-PATTERNS.md (section src/components/FullBleedImage.astro — Path A target pattern)
    - .planning/phases/05-responsive-design/05-RESEARCH.md (section 2 Surface 3 — Path A vs Path B decision; Open Question 1 — Path A recommendation for Phase 5)
  </read_first>
  <action>
In `src/components/FullBleedImage.astro`, locate the `<img>` tag (PATTERNS.md cites line 21). Replace the current single-line form:

  <img src={src} alt={alt} loading="lazy" decoding="async" />

with the multi-line Path A form:

  <img
    src={src}
    alt={alt}
    loading="lazy"
    decoding="async"
    sizes="100vw"
    class="w-full h-auto block"
  />

Two attributes are added: `sizes="100vw"` and `class="w-full h-auto block"`. The class set ensures the `<img>` fills its `.full-bleed` figure container (which is `width: 100vw` per the scoped CSS).

Preserve verbatim:
- The Astro frontmatter (`Props` interface with `src: string` — Path A KEEPS the string-path interface; do NOT change to `ImageMetadata`)
- The `<figure class="full-bleed my-[var(--spacing-2xl)]">` wrapper
- The scoped `<style>` block (if plan 05-02 already added `overflow-x: hidden` to `.full-bleed`, it stays; if 05-02 has not landed yet, do NOT preemptively add it — that is 05-02's territory)
- Any component-comment lines outside the `<img>` tag area
- `loading="lazy"` and `decoding="async"` (already present — keep them)

Do NOT migrate the `src` prop to `ImageMetadata`. Per RESEARCH Open Question 1: Path B (`src/assets/` migration) is deferred — Phase 5 ships the simplest interpretation. The `sizes="100vw"` attribute on a single-asset `<img>` produces no actual srcset variety (no `srcset` is generated), but it correctly hints the browser about the rendered size, which is the canonical responsive-image practice when only one variant exists.

After the edit, confirm via `grep -A 8 '<figure' src/components/FullBleedImage.astro` that the figure / img / style structure is intact.
  </action>
  <verify>
    <automated>grep -F 'sizes="100vw"' src/components/FullBleedImage.astro && grep -F 'class="w-full h-auto block"' src/components/FullBleedImage.astro && grep -F 'loading="lazy"' src/components/FullBleedImage.astro && grep -F 'decoding="async"' src/components/FullBleedImage.astro && grep -F "interface Props {" src/components/FullBleedImage.astro | grep -F "src: string" && npm run build > /tmp/05-05-build2.log 2>&1 && [ $? = 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - sizes="100vw" present on the <img>: `grep -F 'sizes="100vw"' src/components/FullBleedImage.astro` returns a match
    - class="w-full h-auto block" present on the <img>: `grep -F 'class="w-full h-auto block"' src/components/FullBleedImage.astro` returns a match
    - loading="lazy" preserved: `grep -F 'loading="lazy"' src/components/FullBleedImage.astro` returns a match
    - decoding="async" preserved: `grep -F 'decoding="async"' src/components/FullBleedImage.astro` returns a match
    - Props.src is STILL `string` (no Path B migration): `grep -F "src: string" src/components/FullBleedImage.astro` returns a match; `grep -c "ImageMetadata" src/components/FullBleedImage.astro` returns 0
    - figure wrapper preserved: `grep -F 'class="full-bleed my-[var(--spacing-2xl)]"' src/components/FullBleedImage.astro` returns a match
    - No <Image> import added: `grep -c "astro:assets" src/components/FullBleedImage.astro` returns 0
    - Production build passes: `npm run build` exits 0
    - Type check passes: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
The `<img>` in FullBleedImage carries `sizes="100vw"` and `class="w-full h-auto block"`. Props.src is unchanged. The Path B migration is deferred per RESEARCH Open Question 1. Build and typecheck pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register CaseImage in [id].astro components prop</name>
  <files>src/pages/projects/[id].astro</files>
  <read_first>
    - src/pages/projects/[id].astro (read the file — locate the existing `import FullBleedImage` line at the top and the `<Content components={{ FullBleedImage }} />` invocation near line 91)
    - .planning/phases/05-responsive-design/05-PATTERNS.md (section src/pages/projects/[id].astro — CaseImage registration block, lines 376-383)
  </read_first>
  <action>
In `src/pages/projects/[id].astro`, make exactly two surgical edits:

1. Add a new import line alongside the existing `FullBleedImage` import (PATTERNS.md notes the FullBleedImage import is near line 3 — add the CaseImage import as a sibling line, same import-path depth `../../components/`):

  import CaseImage from '../../components/CaseImage.astro';

2. Update the `<Content components={{ FullBleedImage }} />` invocation (PATTERNS.md cites line 91) to include CaseImage:

   Replace:

     <Content components={{ FullBleedImage }} />

   With:

     <Content components={{ FullBleedImage, CaseImage }} />

Preserve verbatim:
- The entry data destructuring at the top of the page
- The page-header section, summary rendering, prev/next navigation
- The cover-image `<Image>` block (plan 05-04 owns its widths refinement; do not touch that here)
- Every other import and component reference

Do NOT add CaseImage references to any MDX file in this task — that is Task 4's job (documentation-comment only).
  </action>
  <verify>
    <automated>grep -F "import CaseImage from '../../components/CaseImage.astro'" "src/pages/projects/[id].astro" && grep -F "components={{ FullBleedImage, CaseImage }}" "src/pages/projects/[id].astro" && [ "$(grep -c "components={{ FullBleedImage }}" "src/pages/projects/[id].astro")" = "0" ] && npm run build > /tmp/05-05-build3.log 2>&1 && [ $? = 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - CaseImage import added: `grep -F "import CaseImage from '../../components/CaseImage.astro'" "src/pages/projects/[id].astro"` returns a match
    - components prop updated: `grep -F "components={{ FullBleedImage, CaseImage }}" "src/pages/projects/[id].astro"` returns a match
    - Old components prop no longer present: `grep -c "components={{ FullBleedImage }}" "src/pages/projects/[id].astro"` returns 0
    - FullBleedImage import preserved: `grep -F "import FullBleedImage" "src/pages/projects/[id].astro"` returns a match
    - Production build passes: `npm run build` exits 0 (any MDX file referencing `<CaseImage>` would now resolve — none do yet, but the registration is in place)
    - Type check passes: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
`[id].astro` imports CaseImage and registers it on the `<Content components={...}>` prop alongside FullBleedImage. Existing FullBleedImage usage is unaffected. Build and typecheck pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add CaseImage usage documentation comment to project-alpha.mdx template</name>
  <files>src/content/projects/project-alpha.mdx</files>
  <read_first>
    - src/content/projects/project-alpha.mdx (read the full file — confirm the existing top-of-file comment that documents `<FullBleedImage>` usage, per PATTERNS.md lines 13-15)
    - .planning/phases/05-responsive-design/05-PATTERNS.md (section src/content/projects/*.mdx — usage pattern parallel to FullBleedImage)
    - .planning/phases/05-responsive-design/05-RESEARCH.md (section 6 Option A — MDX usage shape)
  </read_first>
  <action>
In `src/content/projects/project-alpha.mdx`, locate the existing top-of-file MDX comment that documents the `<FullBleedImage>` usage pattern. Append a parallel comment block documenting `<CaseImage>` usage. MDX comments use `{/* ... */}` JSX syntax (NOT `<!-- -->` HTML syntax).

Add this comment block immediately after the existing FullBleedImage documentation comment:

  {/* CASE BODY IMAGE PATTERN — for inline images inside the prose body (not full-bleed):
       1. Import the image at the top of the MDX file:
            import CaseImage from '../../components/CaseImage.astro';
            import myImg from './my-image.jpg';
       2. Use the component:
            <CaseImage src={myImg} alt="Descriptive alt text" caption="Optional caption" />
       The wrapper applies widths={[400, 720, 1440]} and sizes for the 720px prose column automatically.
       Use FullBleedImage for full-viewport-width breakout images instead. */}

Constraints:
- The comment is a JSX-style MDX comment: `{/* ... */}`
- It does NOT add a real `<CaseImage>` usage to the file (no MDX content is rewritten in Phase 5)
- It does NOT remove or modify the existing `<FullBleedImage>` documentation comment
- It does NOT modify the actual `<FullBleedImage>` invocation lower in the file
- It does NOT add any import statements (the comment shows them as text only)

Only `project-alpha.mdx` receives this update. The other four MDX files (`project-beta` through `project-epsilon`) are placeholder content; Tanya can read the canonical pattern from `project-alpha.mdx` when she writes real content. This avoids 5 identical comment blocks scattered across files.

After editing, confirm `npm run build` still passes — MDX parsing errors would surface as build failures.
  </action>
  <verify>
    <automated>grep -F "CASE BODY IMAGE PATTERN" src/content/projects/project-alpha.mdx && grep -F "CaseImage from '../../components/CaseImage.astro'" src/content/projects/project-alpha.mdx && grep -F "widths={[400, 720, 1440]}" src/content/projects/project-alpha.mdx && npm run build > /tmp/05-05-build4.log 2>&1 && [ $? = 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - New comment block present: `grep -F "CASE BODY IMAGE PATTERN" src/content/projects/project-alpha.mdx` returns a match
    - Import-line example in comment: `grep -F "CaseImage from '../../components/CaseImage.astro'" src/content/projects/project-alpha.mdx` returns a match (inside the comment block as documentation text)
    - widths array example documented: `grep -F "widths={[400, 720, 1440]}" src/content/projects/project-alpha.mdx` returns a match (inside the comment block)
    - Existing FullBleedImage pattern comment preserved: `grep -F "FullBleedImage" src/content/projects/project-alpha.mdx` returns at least two matches (existing comment + existing JSX usage at line 27)
    - Existing FullBleedImage JSX usage UNCHANGED: `grep -F '<FullBleedImage src="/images/project-alpha-process.jpg"' src/content/projects/project-alpha.mdx` returns a match
    - No real `<CaseImage>` JSX added (this task is doc-only): `grep -c "^<CaseImage" src/content/projects/project-alpha.mdx` returns 0
    - MDX parses: `npm run build` exits 0
    - Type check passes: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
`project-alpha.mdx` documents both `<FullBleedImage>` (existing comment) and `<CaseImage>` (new comment) usage at the top of the file. No real content is added or removed. Build and typecheck pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Record image-pipeline plan results in 05-AUDIT.md</name>
  <files>.planning/phases/05-responsive-design/05-AUDIT.md</files>
  <read_first>
    - .planning/phases/05-responsive-design/05-AUDIT.md (locate the `FullBleedImage (in case prose)` row — plan 05-02 already marked its overflow column resolved; this task adds the Images? column update)
  </read_first>
  <action>
After Tasks 1 through 4 land, capture commit SHAs and update `05-AUDIT.md`:

1. `FullBleedImage (in case prose)` row — Images? column: append `PASS (sizes=100vw — Path A; Path B migration deferred)`. Notes: append `commit <SHA>` for whichever commit contains the FullBleedImage `<img>` edit (Task 2).

2. Add a NEW row at the bottom of the matrix table (between the existing `FullBleedImage` row and the `ArrowLink` row, or appended after `ArrowLink` if cleaner): a row for `CaseImage component (MDX wrapper)` with cells:
   - 375: `PASS (preparedness — no real MDX usage yet)`
   - 768: `PASS (preparedness)`
   - 1024: `PASS (preparedness)`
   - 1440: `PASS (preparedness)`
   - Images? column: `PASS (widths=[400,720,1440], sizes for 720px column)`
   - Notes: `Wrapper component + registration + doc-comment in project-alpha.mdx (commits <Task-1-SHA>, <Task-3-SHA>, <Task-4-SHA>)`

3. Do NOT modify the row data for `about.astro — hero grid` or `projects/[id].astro — cover image` — plan 05-04 already wrote their Image-column values.
  </action>
  <verify>
    <automated>grep -F "FullBleedImage (in case prose)" .planning/phases/05-responsive-design/05-AUDIT.md | grep -F "Path A" && grep -F "CaseImage component (MDX wrapper)" .planning/phases/05-responsive-design/05-AUDIT.md && grep -F "preparedness" .planning/phases/05-responsive-design/05-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - FullBleedImage row Images? column updated: `grep "FullBleedImage (in case prose)" .planning/phases/05-responsive-design/05-AUDIT.md` line includes `Path A`
    - New CaseImage row exists: `grep -F "CaseImage component (MDX wrapper)" .planning/phases/05-responsive-design/05-AUDIT.md` returns a match
    - preparedness annotation present in CaseImage row: `grep -F "preparedness" .planning/phases/05-responsive-design/05-AUDIT.md` returns at least one match
    - Three commit SHAs cited in CaseImage row Notes: `grep -E "commits [0-9a-f]{7}" .planning/phases/05-responsive-design/05-AUDIT.md` returns at least one match
    - Other rows untouched by this task: the `about.astro — hero grid` Image column still reads `4-step widths` (set by plan 05-04, not overwritten)
  </acceptance_criteria>
  <done>
Audit matrix records FullBleedImage Path A completion and adds a new CaseImage preparedness row. Commit SHAs are cited.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX → Astro components prop | `<Content components={{ FullBleedImage, CaseImage }}>` exposes Astro components to MDX-authored content. Tanya is the sole MDX author; no external content. |
| Astro build pipeline (sharp) | CaseImage's `src` is an `ImageMetadata` (local-asset import). sharp processes it at build time. No runtime image fetching. |
| FullBleedImage `src` (public/ string) | Per CONTEXT.md threat model: "Image source integrity (any image migrated public/ → src/assets/ must still be the same asset, not silently replaced)". This plan does NOT migrate — it only adds `sizes="100vw"` to the existing `<img>` whose src remains a public path. No integrity surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | T (Tampering) | CaseImage Props.src type (`ImageMetadata`) | mitigate | TypeScript enforces `ImageMetadata` at the call site; MDX authors cannot pass an arbitrary string. Astro's content collection schema additionally validates `thumbnail` images, but CaseImage takes inline imports — Tanya must import the file. Mitigation: the typecheck step in acceptance criteria fails any non-`ImageMetadata` argument. |
| T-05-05-02 | I (Information disclosure) | FullBleedImage `src` (public path string) | accept | Same trust boundary as the pre-existing code. No additional disclosure surface. Per CONTEXT.md threat model: this plan does NOT migrate assets between `public/` and `src/assets/`; the integrity-of-source threat is not engaged. |
| T-05-05-03 | T (Tampering) | MDX content injection via raw HTML | mitigate | CONTEXT.md threat model raises this concern. Astro 6 MDX renders `<FullBleedImage>` and `<CaseImage>` as Astro components, not as `dangerouslySetInnerHTML` strings. The components themselves render Astro `<Image>` / `<img>` whose attributes are interpolated via JSX, not concatenated as HTML. No raw HTML injection surface introduced by this plan. (Verified via PATTERNS.md sample showing JSX-interpolated attributes.) |
| T-05-05-04 | D (Denial of service) | New CaseImage component build cost | accept | Negligible — one extra Astro `<Image>` per MDX usage, future-tense (no current usages). |

Severity: all low. No high-severity threats. `security_enforcement` gate passes.
</threat_model>

<verification>
- `src/components/CaseImage.astro` exists with exact template; typecheck and build pass.
- FullBleedImage `<img>` carries `sizes="100vw"` and `class="w-full h-auto block"`; Props.src still `string` (Path A).
- `[id].astro` imports CaseImage and registers `{ FullBleedImage, CaseImage }` on the `<Content>` components prop.
- `project-alpha.mdx` contains the CaseImage usage-pattern comment without adding any real CaseImage JSX.
- Audit matrix records FullBleedImage Path A completion and the new CaseImage preparedness row.
- `npm run build` and `npm run typecheck` exit 0 after every task.
</verification>

<success_criteria>
- All four D-06 image surfaces are addressed: About hero (05-04), case study cover (05-04), FullBleedImage (this plan), MDX inline images (this plan via CaseImage wrapper).
- The CaseImage wrapper bakes in the prose-column widths/sizes so Tanya never types them.
- Path B (`src/assets/` migration for FullBleedImage) is documented as deferred in the audit matrix Notes column.
- Build and typecheck pass after every task.
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-design/05-05-SUMMARY.md` summarizing: CaseImage created, FullBleedImage Path A applied, MDX doc-comment added, audit-matrix updates, deferred Path B note.
</output>
