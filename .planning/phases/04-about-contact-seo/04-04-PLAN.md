---
phase: 04-about-contact-seo
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/pages/index.astro
  - src/pages/projects/[id].astro
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03]
tags: [seo, meta, per-page]

must_haves:
  truths:
    - "Homepage passes title, description, and ogImage props to BaseLayout"
    - "Case study dynamic route passes per-project title (with 'Tanya Zakus, UX/UI Designer' suffix), description (from summary frontmatter), ogImage, and ogType='article' to BaseLayout"
    - "About page (created in Plan 02) already passes title + description — verified, no changes needed in this plan"
    - "Every page's <title> includes 'Tanya Zakus' AND 'UX/UI Designer' (SEO-01)"
    - "Every page emits a meta description appropriate to that page (SEO-02)"
    - "Every page emits og:title, og:description, og:image specific to that page (SEO-03)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage BaseLayout invocation extended with description + ogImage"
      contains: 'description='
    - path: "src/pages/projects/[id].astro"
      provides: "Case study dynamic route BaseLayout invocation extended with description (from summary), ogImage, ogType=article, and title suffix corrected"
      contains: 'ogType="article"'
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro Props interface (extended in Plan 01)"
      via: "<BaseLayout title='...' description='...' ogImage='...'>"
      pattern: 'description='
    - from: "src/pages/projects/[id].astro"
      to: "src/layouts/BaseLayout.astro Props interface (extended in Plan 01)"
      via: "<BaseLayout title={...} description={entry.data.summary} ogImage='...' ogType='article'>"
      pattern: 'entry\\.data\\.summary'
---

<objective>
Wire per-page SEO content through to the BaseLayout Props interface (extended in Plan 01). Homepage and case study dynamic route currently pass only `title`. After this plan, every page passes explicit `title` + `description` + `ogImage` (and `ogType="article"` for case studies), so the new OG / Twitter Card / canonical meta added in Plan 01 emits accurate per-page values.

Per-page values per D-19:
- Homepage → `title="Tanya Zakus — UX/UI Designer"`, `description=` portfolio-focus summary, default ogImage
- Case study route → `title={`${title} — Tanya Zakus, UX/UI Designer`}`, `description={summary frontmatter field}`, default ogImage, `ogType="article"`
- About → already passes title + description in Plan 02 (verified, no edit)

Purpose: Satisfy SEO-01 (every page title includes name + role), SEO-02 (every page has meta description), SEO-03 (Open Graph values are specific per page so LinkedIn / Twitter previews are accurate).

Output: Two modified page files (index.astro + projects/[id].astro). About is verified-only. No new files.

Why Wave 2: depends on Plan 01 (BaseLayout Props must accept ogImage / ogType / canonical) and Plan 02 (about.astro must exist; while this plan does not edit about.astro, the verification step checks all three pages pass description so we need about.astro present for the build-output check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-about-contact-seo/04-CONTEXT.md
@.planning/phases/04-about-contact-seo/04-PATTERNS.md
@.planning/phases/04-about-contact-seo/04-01-SUMMARY.md
@.planning/phases/04-about-contact-seo/04-02-SUMMARY.md
@CLAUDE.md
@src/pages/index.astro
@src/pages/projects/[id].astro

<interfaces>
<!-- BaseLayout Props (extended in Plan 01) -->
```ts
interface Props {
  title?: string;
  description?: string;
  ogImage?: string;       // defaults to "/og-image.png"
  ogType?: 'website' | 'article';  // defaults to "website"
  canonical?: string;     // auto-derived from Astro.url + Astro.site if omitted
}
```

<!-- Homepage current invocation (src/pages/index.astro:19) -->
```astro
<BaseLayout title="Tanya Zakus — UX/UI Designer">
```

<!-- Case study current invocation (src/pages/projects/[id].astro:40) -->
```astro
<BaseLayout title={`${entry.data.title} — Tanya Zakus`}>
```

<!-- Case study `entry.data.summary` field is already in the schema and used at line 58 for the page summary paragraph -->
```astro
<p class="...">
  {entry.data.summary}
</p>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend BaseLayout invocation in src/pages/index.astro with description + ogImage</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (verify current BaseLayout invocation at line 19)
    - .planning/phases/04-about-contact-seo/04-CONTEXT.md (D-19 homepage values)
    - .planning/phases/04-about-contact-seo/04-PATTERNS.md (index.astro section, lines 292-310)
    - .planning/phases/04-about-contact-seo/04-01-SUMMARY.md (confirm BaseLayout Props extension landed in Wave 1)
  </read_first>
  <action>
Edit `src/pages/index.astro` line 19. Replace the single-prop BaseLayout invocation with a multi-prop invocation. The closing tag at line 69 stays as-is.

**Replace line 19 exactly:**
```astro
<BaseLayout title="Tanya Zakus — UX/UI Designer">
```

**With this multi-line invocation:**
```astro
<BaseLayout
  title="Tanya Zakus — UX/UI Designer"
  description="UX/UI designer with 6+ years building humanist AI experiences and complex SaaS platforms. Selected case studies and current availability."
  ogImage="/og-image.png"
>
```

The description is ≤160 chars (157 chars). It mentions "UX/UI designer" and the noun "case studies" per the `<specifics>` description-copy guidance. `ogType` defaults to `"website"` in BaseLayout — no need to pass it on the homepage.

**Do NOT touch:**
- The frontmatter (lines 1-17)
- The hero section (lines 21-52)
- The projects section (lines 54-67)
- The closing `</BaseLayout>` (line 69)
- The script block (lines 71-74)
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && grep -c 'og:description' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'description="UX/UI designer with 6+ years' src/pages/index.astro` returns 1 (NEW prop the edit adds — change-detecting)
    - `grep -c 'ogImage="/og-image.png"' src/pages/index.astro` returns 1 (NEW prop the edit adds — change-detecting)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - After build, `grep -c 'UX/UI designer with 6+ years' dist/index.html` returns at least 1 (description renders)
    - After build, `grep -c 'og:description' dist/index.html` returns at least 1 (OG description meta emits)
    - After build, `grep -c 'og:image' dist/index.html` returns at least 1
  </acceptance_criteria>
  <done>Homepage passes title + description + ogImage to BaseLayout. Built dist/index.html contains all expected meta tags. typecheck + build pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend BaseLayout invocation in src/pages/projects/[id].astro with description, ogImage, ogType=article and corrected title suffix</name>
  <files>src/pages/projects/[id].astro</files>
  <read_first>
    - src/pages/projects/[id].astro (verify current BaseLayout invocation at line 40 and that `entry.data.summary` is referenced at line 58)
    - src/content.config.ts (verify `summary` field exists in projects schema — used as the description value)
    - .planning/phases/04-about-contact-seo/04-CONTEXT.md (D-19 case study route values)
    - .planning/phases/04-about-contact-seo/04-PATTERNS.md (projects/[id].astro section, lines 314-333)
    - .planning/phases/04-about-contact-seo/04-01-SUMMARY.md (confirm BaseLayout Props ogType="article" lands in Wave 1)
  </read_first>
  <action>
Edit `src/pages/projects/[id].astro` line 40. Replace the single-prop BaseLayout invocation with a multi-prop invocation, also fixing the title suffix per D-19 (current `— Tanya Zakus` becomes `— Tanya Zakus, UX/UI Designer` to satisfy SEO-01's name + role rule). Closing tag at line 109 stays.

**Replace line 40 exactly:**
```astro
<BaseLayout title={`${entry.data.title} — Tanya Zakus`}>
```

**With this multi-line invocation:**
```astro
<BaseLayout
  title={`${entry.data.title} — Tanya Zakus, UX/UI Designer`}
  description={entry.data.summary}
  ogImage="/og-image.png"
  ogType="article"
>
```

Two functional changes:
1. Title suffix: `— Tanya Zakus` → `— Tanya Zakus, UX/UI Designer` (D-19, SEO-01).
2. New props: `description={entry.data.summary}`, `ogImage="/og-image.png"`, `ogType="article"`.

The `summary` frontmatter field is already validated by `src/content.config.ts` — no schema change. Reusing it as the meta description is per D-19 (no schema change needed).

`ogType="article"` distinguishes case study pages from `website`-type pages (homepage, About). LinkedIn/Twitter render slightly different cards for `article` (with publish date, byline) vs `website`.

**Do NOT touch:**
- The `getStaticPaths` function (lines 9-23)
- The Props type or destructure (lines 25-31)
- The `Content` render and `timeline` derivation (lines 32-37)
- Any of the body markup (lines 42-107)
- The closing `</BaseLayout>` (line 109)
- The `<style is:global>` block (lines 111-138)

**Note on ogImage:** D-18 specifies a single site-wide OG image. Per-case-study OG images are explicitly deferred (CONTEXT.md `<deferred>` first item: "Per-case-study OG images... belongs in v2 polish"). Pass the default `/og-image.png` here.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && find dist/projects -name "index.html" | head -1 | xargs grep -c 'og:type" content="article"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'description={entry.data.summary}' src/pages/projects/\[id\].astro` returns 1
    - `grep -c 'ogImage="/og-image.png"' src/pages/projects/\[id\].astro` returns 1
    - `grep -c 'ogType="article"' src/pages/projects/\[id\].astro` returns 1
    - `grep -c 'Tanya Zakus, UX/UI Designer' src/pages/projects/\[id\].astro` returns 1
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - After build, at least one case study page exists at `dist/projects/*/index.html` (proves dynamic route generates)
    - After build, the first case study HTML contains `og:type" content="article"` (grep returns at least 1)
    - After build, the first case study HTML contains the project's `summary` text in a `meta name="description"` tag
  </acceptance_criteria>
  <done>Case study dynamic route passes per-project title (with full UX/UI Designer suffix), description (from summary), ogImage, and ogType=article to BaseLayout. Built dist/projects/{slug}/index.html contains all expected meta tags. typecheck + build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX frontmatter → HTML meta | Case study `entry.data.summary` is author-controlled (Tanya writes the MDX) and validated by Zod schema in `src/content.config.ts`. |
| build-time prop pass-through | Homepage description / ogImage strings are hardcoded in `index.astro`. No runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | T (Tampering) / HTML injection via summary | `description={entry.data.summary}` interpolation into `<meta name="description">` | accept | `summary` is author-controlled (Tanya writes MDX frontmatter, validated by Zod schema). Astro auto-escapes attribute values via JSX-style interpolation — even if a malicious string contained HTML, it would render as encoded text inside `content="..."`. No XSS vector. |
| T-04-10 | I (Information disclosure) via OG meta | OG meta exposes page title / description publicly | accept | Page titles and descriptions are intentionally public (their purpose is to appear in search results and social previews). |
</threat_model>

<verification>
After both tasks land:
1. `npm run build` succeeds with no warnings.
2. `dist/index.html` contains `<title>Tanya Zakus — UX/UI Designer</title>` and `<meta name="description" content="UX/UI designer with 6+ years...">` and `<meta property="og:type" content="website">`.
3. For each case study file in `dist/projects/`, the HTML contains:
   - `<title>{Project title} — Tanya Zakus, UX/UI Designer</title>`
   - `<meta name="description" content="{project summary}">`
   - `<meta property="og:type" content="article">`
4. `dist/about/index.html` (created by Plan 02) contains its own title + description meta — verified at the dist level only, no edit to about.astro in this plan.
5. Run a 2-page visual sanity check in dev mode: open homepage and a case study, view source, confirm head meta tags are correct.
</verification>

<success_criteria>
- [ ] Homepage BaseLayout invocation passes title + description + ogImage
- [ ] Case study dynamic route BaseLayout invocation passes title (with full UX/UI Designer suffix) + description (from entry.data.summary) + ogImage + ogType="article"
- [ ] Built dist/index.html has all 7 OG meta + 4 Twitter Card meta + canonical link with homepage values
- [ ] Built dist/projects/*/index.html has all the same meta with per-project values
- [ ] Built dist/about/index.html has all the same meta with about-page values (covered by Plan 02 + Plan 01)
- [ ] `npm run typecheck` and `npm run build` exit 0
- [ ] Every page's `<title>` includes "Tanya Zakus" AND "UX/UI Designer" (SEO-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-about-contact-seo/04-04-SUMMARY.md` documenting:
- Final invocation block for index.astro (paste)
- Final invocation block for projects/[id].astro (paste)
- Confirmation that about.astro from Plan 02 already passes title + description (no edit needed)
- For each of homepage / about / one case study, paste the `<title>`, `<meta name="description">`, `<meta property="og:title">`, `<meta property="og:type">`, and `<link rel="canonical">` lines from the built `dist/.../index.html`
- Any deviations from D-19
</output>
</content>
