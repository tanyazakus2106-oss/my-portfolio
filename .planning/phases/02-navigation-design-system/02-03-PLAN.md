---
phase: 02-navigation-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/Footer.astro
autonomous: true
requirements:
  - FOOT-01
  - FOOT-02
  - FOOT-03
  - POL-03
  - POL-04
must_haves:
  truths:
    - "Footer displays a two-column grid at >=768px: left column = 'Let's work together.' + 'Get in touch →' mailto + LinkedIn icon, right column = stacked nav links"
    - "On mobile (<768px), footer stacks into a single column"
    - "Footer nav links mirror the header exactly: Work, About, Resume, LinkedIn"
    - "Get in touch link is wired to mailto:tanyazakus2106@gmail.com"
    - "LinkedIn link opens in a new tab with rel='noopener noreferrer'"
    - "All padding/margin/gap values use --spacing-* tokens (no hardcoded px)"
    - "Typography uses DM Sans 400 (body) and 600 (heading) — no other weights"
    - "Copyright line © 2026 Tanya Zakus remains visible at the bottom"
  artifacts:
    - path: "src/components/Footer.astro"
      provides: "Two-column footer with CTA, social links, mirrored nav, copyright"
      contains: "grid-cols-1 md:grid-cols-2"
  key_links:
    - from: "Footer 'Get in touch' link"
      to: "tanyazakus2106@gmail.com"
      via: "mailto:"
      pattern: "mailto:tanyazakus2106@gmail.com"
    - from: "Footer LinkedIn link"
      to: "linkedin.com profile"
      via: "external anchor with target=_blank rel=noopener noreferrer"
      pattern: "rel=\"noopener noreferrer\""
    - from: "Footer nav"
      to: "Same links as Header.astro (Work, About, Resume, LinkedIn)"
      via: "hardcoded list mirroring header copy contract"
      pattern: "Work|About|Resume|LinkedIn"
---

<objective>
Transform the placeholder Footer.astro (from Plan 01) into the final two-column layout: left column with "Let's work together." heading, mailto CTA, and LinkedIn icon; right column with stacked nav links mirroring the header. Runs in Wave 2 in parallel with Plan 02 (MobileNav) — they modify different files.

Purpose: Satisfy FOOT-01 (footer mirrors nav links), FOOT-02 (social links — LinkedIn + email), FOOT-03 (Get in touch CTA + closing line), POL-03/POL-04 (token-based typography and spacing).

Output: Fully refactored `src/components/Footer.astro` with responsive grid, mailto CTA, LinkedIn + email icon links, mirrored nav column, and preserved copyright.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-navigation-design-system/02-CONTEXT.md
@.planning/phases/02-navigation-design-system/02-UI-SPEC.md
@.planning/phases/02-navigation-design-system/02-01-SUMMARY.md
@src/components/Footer.astro
@src/styles/global.css

<interfaces>
<!-- Copywriting Contract (Phase 2 UI-SPEC) — exact strings required -->
Footer heading: "Let's work together."   (24px / 600, --color-text-primary)
Footer CTA:     "Get in touch →"          (--color-accent)
Footer email:   tanyazakus2106@gmail.com  (target of mailto)
LinkedIn URL:   https://linkedin.com      (external, new tab)
Copyright:      © 2026 Tanya Zakus        (14px / 400, --color-text-secondary)

<!-- Mirror header link set exactly (from Plan 01 Header.astro): -->
Work     → /work           (internal)
About    → /about          (internal)
Resume   → /resume.pdf     (internal download)
LinkedIn → https://linkedin.com (external, target="_blank" rel="noopener noreferrer")

<!-- Spacing tokens (from global.css) -->
--spacing-sm (8px), --spacing-md (16px), --spacing-lg (24px),
--spacing-xl (32px), --spacing-2xl (48px), --spacing-3xl (64px)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite Footer.astro as a two-column grid with mirrored nav, mailto CTA, LinkedIn + email icons, and copyright</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/components/Footer.astro (current placeholder from Plan 01)
    - src/components/Header.astro (confirm link list parity for FOOT-01 mirror)
    - .planning/phases/02-navigation-design-system/02-UI-SPEC.md (Footer contract, Copywriting Contract, Accessibility Baseline)
    - .planning/phases/02-navigation-design-system/02-CONTEXT.md (D-13, D-14, D-15, D-16)
  </read_first>
  <action>
Fully rewrite `src/components/Footer.astro`. Do not preserve the placeholder from Plan 01 — replace wholesale.

Required final content:

```astro
 ---
const navLinks = [
  { href: '/work', label: 'Work', external: false },
  { href: '/about', label: 'About', external: false },
  { href: '/resume.pdf', label: 'Resume', external: false },
  { href: 'https://linkedin.com', label: 'LinkedIn', external: true },
];

const EMAIL = 'tanyazakus2106@gmail.com';
const LINKEDIN_URL = 'https://linkedin.com';
 ---

<footer class="bg-[var(--color-secondary)] border-t border-[var(--color-border)] py-[var(--spacing-2xl)]">
  <div class="max-w-[1200px] mx-auto px-[var(--spacing-lg)] md:px-[var(--spacing-2xl)] lg:px-[var(--spacing-3xl)]">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-[var(--spacing-xl)]">

      <!-- Left column: CTA + social icons -->
      <div class="flex flex-col gap-[var(--spacing-md)]">
        <p class="text-2xl font-semibold leading-[1.25] text-[var(--color-text-primary)]">
          Let's work together.
        </p>
        <a
          href={`mailto:${EMAIL}`}
          class="inline-flex items-center min-h-[44px] text-base font-normal text-[var(--color-accent)] hover:underline focus-visible:outline-2 focus-visible:outline-[var(--color-accent)] focus-visible:outline-offset-2 w-fit"
        >
          Get in touch →
        </a>

        <!-- Social icon links — FOOT-02: LinkedIn + email -->
        <ul class="flex items-center gap-[var(--spacing-md)] mt-[var(--spacing-sm)]" aria-label="Social links">
          <li>
            <a
              href={LINKEDIN_URL}
              target="_blank"
              rel="noopener noreferrer"
              aria-label="LinkedIn profile"
              class="min-h-[44px] min-w-[44px] inline-flex items-center justify-center rounded-md text-[var(--color-text-primary)] hover:text-[var(--color-accent)] focus-visible:outline-2 focus-visible:outline-[var(--color-accent)] focus-visible:outline-offset-2 transition-colors"
            >
              <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M20.45 20.45h-3.56v-5.57c0-1.33-.03-3.04-1.85-3.04-1.85 0-2.14 1.45-2.14 2.95v5.66H9.34V9h3.42v1.56h.05c.48-.9 1.64-1.85 3.37-1.85 3.6 0 4.27 2.37 4.27 5.46v6.28zM5.34 7.43a2.07 2.07 0 1 1 0-4.13 2.07 2.07 0 0 1 0 4.13zM7.12 20.45H3.56V9h3.56v11.45zM22.22 0H1.77C.79 0 0 .77 0 1.72v20.56C0 23.23.79 24 1.77 24h20.45c.98 0 1.78-.77 1.78-1.72V1.72C24 .77 23.2 0 22.22 0z"/>
              </svg>
            </a>
          </li>
          <li>
            <a
              href={`mailto:${EMAIL}`}
              aria-label="Email Tanya"
              class="min-h-[44px] min-w-[44px] inline-flex items-center justify-center rounded-md text-[var(--color-text-primary)] hover:text-[var(--color-accent)] focus-visible:outline-2 focus-visible:outline-[var(--color-accent)] focus-visible:outline-offset-2 transition-colors"
            >
              <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="2" y="4" width="20" height="16" rx="2"/>
                <path d="m22 7-10 6L2 7"/>
              </svg>
            </a>
          </li>
        </ul>
      </div>

      <!-- Right column: mirrored nav links (FOOT-01) -->
      <nav aria-label="Footer navigation" class="md:justify-self-end">
        <ul class="flex flex-col gap-[var(--spacing-sm)]">
          {navLinks.map((link) => (
            <li>
              <a
                href={link.href}
                {...(link.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
                class="inline-flex items-center min-h-[44px] text-base font-normal text-[var(--color-text-primary)] hover:text-[var(--color-accent)] focus-visible:outline-2 focus-visible:outline-[var(--color-accent)] focus-visible:outline-offset-2 transition-colors"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>

    </div>

    <!-- Copyright row -->
    <p class="mt-[var(--spacing-2xl)] text-sm font-normal text-[var(--color-text-secondary)]">
      &copy; 2026 Tanya Zakus
    </p>
  </div>
</footer>
```

Key compliance points:
- D-13 two-column layout: `grid-cols-1 md:grid-cols-2`, left = CTA + socials, right = nav mirror
- D-14 email wired: `mailto:tanyazakus2106@gmail.com` on both CTA and email icon
- D-15 social links live in left column beneath the CTA (LinkedIn icon + email icon)
- D-16 copyright "© 2026 Tanya Zakus" preserved at bottom
- FOOT-01 nav mirror: exact same four links as Header (same copy strings, same hrefs)
- FOOT-02 LinkedIn + email social icons with aria-labels
- FOOT-03 "Get in touch →" CTA + "Let's work together." closing line
- POL-03 typography: only `font-normal` (400) and `font-semibold` (600) used; sizes use Tailwind's 16px base (`text-base`, `text-2xl` = 24px heading, `text-sm` = 14px copyright)
- POL-04 spacing: every padding/gap/margin uses a `--spacing-*` token or Tailwind's token-aligned utility (no arbitrary px values)
- Security: external LinkedIn gets `rel="noopener noreferrer"`
- Accessibility: 44px touch targets, aria-labels on icon-only links, focus-visible rings
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Footer.astro contains `grid-cols-1 md:grid-cols-2`
    - Footer.astro contains `Let's work together.`
    - Footer.astro contains `Get in touch →`
    - Footer.astro contains `mailto:tanyazakus2106@gmail.com` (at least twice — CTA + email icon)
    - Footer.astro contains `aria-label="Footer navigation"`
    - Footer.astro contains `aria-label="LinkedIn profile"`
    - Footer.astro contains `aria-label="Email Tanya"`
    - Footer.astro contains `aria-label="Social links"`
    - Footer.astro contains `rel="noopener noreferrer"` (for LinkedIn links)
    - Footer.astro contains `target="_blank"` (for LinkedIn links)
    - Footer.astro contains all four mirrored labels: `Work`, `About`, `Resume`, `LinkedIn`
    - Footer.astro contains `&copy; 2026 Tanya Zakus`
    - Footer.astro contains `bg-[var(--color-secondary)]`
    - Footer.astro contains `py-[var(--spacing-2xl)]`
    - Footer.astro contains `text-[var(--color-accent)]` (CTA color)
    - Footer.astro contains `min-h-[44px]` on interactive elements
    - Footer.astro does NOT contain any hex color (grep `#[0-9A-Fa-f]{6}` returns no matches)
    - Footer.astro does NOT contain arbitrary pixel values outside the 4px scale — no `px-[7px]`, `py-[13px]` style arbitrary values; all spacing uses `--spacing-*` tokens or Tailwind spacing scale
    - Footer.astro uses only `font-normal` and `font-semibold` (no font-medium, font-bold, font-light)
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>Footer.astro is the final two-column layout. Every token requirement satisfied. All acceptance grep checks pass. Build green.</done>
</task>

</tasks>

<verification>
1. `npm run build` — exits 0
2. `npm run dev`, load homepage:
   - At >=768px: footer shows two columns — left has heading + "Get in touch →" + LinkedIn/email icons; right has stacked nav (Work/About/Resume/LinkedIn)
   - At <768px: columns stack into one
   - Click "Get in touch →": mailto opens with tanyazakus2106@gmail.com
   - Click LinkedIn icon: opens https://linkedin.com in new tab (verify `target="_blank"` behavior)
   - Click email icon: mailto opens
3. Toggle dark mode: footer background flips (secondary color in dark = #1A1A1A), accent color on CTA flips (3B82F6), all text remains readable
4. Tab through footer: focus rings appear on all interactive elements
5. Inspect each link: confirms min 44px tap height
6. No visible regression in the header (Plan 02's changes and Plan 03's changes don't conflict — different files)
</verification>

<success_criteria>
- Footer.astro refactored to two-column responsive grid
- CTA, LinkedIn, and email links all functional with correct href/mailto/target/rel
- Mirrored nav links match header exactly (FOOT-01)
- LinkedIn + email social icons present with aria-labels (FOOT-02)
- "Let's work together." + "Get in touch →" + "© 2026 Tanya Zakus" copy preserved (FOOT-03)
- Zero hardcoded colors; zero arbitrary pixel values
- Only font weights 400 and 600 used (POL-03)
- All spacing via --spacing-* tokens (POL-04)
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation-design-system/02-03-SUMMARY.md` documenting:
- Final footer structure and link inventory
- Confirmation that header ↔ footer nav mirrors match exactly
- Token coverage report (any deviations from `--spacing-*` or `--color-*` tokens)
</output>
