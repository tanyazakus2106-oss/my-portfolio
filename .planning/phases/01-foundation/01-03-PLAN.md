---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements:
  - FOUND-04
user_setup:
  - service: cloudflare-pages
    why: "Static site hosting with preview deploys"
    env_vars: []
    dashboard_config:
      - task: "Create Cloudflare Pages project connected to GitHub repository"
        location: "Cloudflare Dashboard -> Workers & Pages -> Create -> Pages -> Connect to Git"
      - task: "Set NODE_VERSION environment variable to 20"
        location: "Cloudflare Pages project -> Settings -> Environment variables"
must_haves:
  truths:
    - "Pushing to main branch triggers a Cloudflare Pages build that succeeds"
    - "The deployed preview URL is publicly accessible and shows the index page"
  artifacts: []
  key_links:
    - from: "GitHub main branch push"
      to: "Cloudflare Pages build"
      via: "GitHub integration webhook"
      pattern: "Build successful"
---

<objective>
Connect the repository to Cloudflare Pages and verify a successful preview deploy is live at a public URL.

Purpose: FOUND-04 requires a working CI/CD pipeline from day one — every subsequent phase's work is deployable the moment it merges. This also validates the build works in Cloudflare's environment (not just locally).
Output: A live Cloudflare Pages preview URL serving the portfolio's index page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize git repository and push to GitHub</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Cloudflare Pages section — build command, output dir)
    - .gitignore (if it exists — verify node_modules and dist are ignored)
  </read_first>
  <action>
1. Initialize git in the project root (if not already initialized):
   ```
   git init
   ```

2. Verify `.gitignore` exists and includes at minimum:
   - `node_modules/`
   - `dist/`
   - `.astro/`
   If missing, create it with these entries.

3. Stage all project files and create initial commit:
   ```
   git add -A
   git commit -m "feat(01): scaffold Astro 6 project with Tailwind v4, MDX, content schema, and layout shell"
   ```

4. Create a GitHub repository using the gh CLI:
   ```
   gh repo create my-portfolio --public --source=. --push
   ```
   If gh CLI is not authenticated, this will prompt authentication. If the repo already exists on GitHub, push to it:
   ```
   git remote add origin https://github.com/USER/my-portfolio.git
   git branch -M main
   git push -u origin main
   ```

5. Verify the push succeeded and the repository is visible on GitHub.
  </action>
  <verify>
    <automated>cd /Users/tanyazakus/Desktop/my-portfolio && git log --oneline -1 && git remote -v && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .git directory exists in project root
    - .gitignore contains `node_modules/`
    - .gitignore contains `dist/`
    - git remote -v shows a GitHub remote URL
    - git log shows at least one commit
    - Main branch is pushed to GitHub
  </acceptance_criteria>
  <done>Git repository initialized, all files committed, and pushed to GitHub. Remote repository is accessible.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect Cloudflare Pages to GitHub repository</name>
  <what-built>The Astro project is built, committed, and pushed to GitHub. Cloudflare Pages needs to be connected to this repository to enable automatic preview deploys on push.</what-built>
  <action>
The user must complete the following steps in the Cloudflare dashboard because Cloudflare Pages project creation requires authenticated dashboard access that cannot be performed via CLI (Wrangler is not installed and dashboard-based GitHub integration is the recommended approach per D-01/D-02).

**Steps for the user:**

1. Go to https://dash.cloudflare.com
2. Navigate to "Workers & Pages" in the left sidebar
3. Click "Create" -> "Pages" -> "Connect to Git"
4. Select the GitHub repository (my-portfolio)
5. Configure build settings:
   - **Framework preset:** Astro
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
6. Under "Environment variables", add:
   - **Variable name:** `NODE_VERSION`
   - **Value:** `20`
7. Click "Save and Deploy"
8. Wait for the first deploy to complete
9. Copy the `.pages.dev` preview URL (e.g., `my-portfolio-xyz.pages.dev`)
  </action>
  <resume-signal>Provide the Cloudflare Pages preview URL (e.g., https://my-portfolio-xyz.pages.dev) and confirm the deploy status shows "Success"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update astro.config.mjs site URL with Cloudflare Pages domain</name>
  <files>astro.config.mjs</files>
  <read_first>
    - astro.config.mjs (current state — verify site field location)
  </read_first>
  <action>
After the user provides the Cloudflare Pages URL from Task 2:

1. Update the `site` field in `astro.config.mjs` from `'https://placeholder.pages.dev'` to the actual Cloudflare Pages URL provided by the user (e.g., `'https://my-portfolio-xyz.pages.dev'`).

2. Commit and push the change:
   ```
   git add astro.config.mjs
   git commit -m "chore(01): update site URL to Cloudflare Pages domain"
   git push
   ```

3. This push will trigger a new Cloudflare Pages build automatically (per D-03 — preview deploy on push to main).
  </action>
  <verify>
    <automated>cd /Users/tanyazakus/Desktop/my-portfolio && grep -q 'pages.dev' astro.config.mjs && ! grep -q 'placeholder.pages.dev' astro.config.mjs && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - astro.config.mjs contains `site:` with a real `.pages.dev` URL (not `placeholder.pages.dev`)
    - Change is committed and pushed to GitHub
    - Cloudflare Pages triggers a new build from the push
  </acceptance_criteria>
  <done>astro.config.mjs site URL updated to the actual Cloudflare Pages domain. Change pushed to main, triggering a new deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub -> Cloudflare Pages | Build pipeline triggered by git push; Cloudflare executes npm run build in its environment |
| Public CDN | Static site served to any visitor from Cloudflare's edge network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Cloudflare Pages build | accept | Build runs in Cloudflare's sandboxed environment; npm lockfile pins dependency versions; no secrets in build |
| T-01-07 | Information Disclosure | Public deploy URL | accept | Site is intentionally public; no private content in Phase 1 (placeholder data only) |
| T-01-08 | Elevation of Privilege | GitHub -> Cloudflare integration | accept | Standard OAuth integration; Cloudflare has read-only access to trigger builds; no write access to repository |
</threat_model>

<verification>
1. Cloudflare Pages dashboard shows "Success" status for the deploy
2. Preview URL is publicly accessible in a browser
3. Visiting the preview URL shows the index page with "Tanya Zakus" header text
4. astro.config.mjs site field contains the actual Cloudflare Pages domain
</verification>

<success_criteria>
- GitHub repository exists with all project files pushed to main branch
- Cloudflare Pages project is connected to the GitHub repository
- Pushing to main triggers an automatic build that succeeds
- A public preview URL (*.pages.dev) serves the portfolio index page
- astro.config.mjs site URL is updated to the actual Cloudflare Pages domain
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
