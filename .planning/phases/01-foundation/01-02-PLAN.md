---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content.config.ts
  - src/content/projects/project-alpha.mdx
  - src/content/projects/project-beta.mdx
  - src/content/projects/thumbnail-alpha.png
  - src/content/projects/thumbnail-beta.png
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - "content.config.ts schema validates 2 placeholder MDX files at build time"
    - "getCollection('projects') returns 2 project entries with all typed frontmatter fields"
    - "Every page is wrapped by BaseLayout with header, main, and footer landmark regions"
    - "Header is sticky with 64px height, frosted glass background, and min 44px touch targets"
    - "Footer has secondary background color and 48px vertical padding"
    - "index.astro renders inside BaseLayout and the build produces dist/index.html"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Content schema with typed frontmatter for projects collection"
      exports: ["collections"]
    - path: "src/content/projects/project-alpha.mdx"
      provides: "Placeholder project 1 with all required frontmatter fields"
      contains: "Project Alpha"
    - path: "src/content/projects/project-beta.mdx"
      provides: "Placeholder project 2 with all required frontmatter fields"
      contains: "Project Beta"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Root layout shell with header, main slot, and footer"
      contains: "sticky top-0 z-50"
    - path: "src/pages/index.astro"
      provides: "Minimal homepage using BaseLayout"
      contains: "BaseLayout"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/projects/*.mdx"
      via: "glob loader pattern"
      pattern: "glob.*projects"
    - from: "src/content/projects/project-alpha.mdx"
      to: "src/content/projects/thumbnail-alpha.png"
      via: "frontmatter thumbnail field"
      pattern: "thumbnail.*thumbnail-alpha"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout import"
      pattern: "import.*BaseLayout"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import in component script"
      pattern: "import.*global"
---

<objective>
Define the project content schema with typed frontmatter, create placeholder MDX files to validate it, and build the root layout shell (BaseLayout.astro) that wraps all pages with header, main, and footer landmark regions.

Purpose: The content schema is the contract that all case study content must satisfy -- validating it now with placeholder data prevents schema surprises in Phase 3. The layout shell is the visual frame inherited by every page in every subsequent phase.
Output: A working content collection with 2 validated placeholder projects, and a BaseLayout wrapping a minimal index page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output -- executor needs these to wire imports correctly -->

From src/styles/global.css (created in Plan 01):
Available CSS custom properties: --color-dominant, --color-secondary, --color-text-primary, --color-text-secondary, --color-border, --color-accent, --color-destructive, --font-sans, --font-mono, --spacing-xs through --spacing-4xl

From astro.config.mjs (created in Plan 01):
MDX integration already configured. site set to 'https://placeholder.pages.dev'. Sitemap integration active.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create content schema and placeholder MDX project files</name>
  <files>src/content.config.ts, src/content/projects/project-alpha.mdx, src/content/projects/project-beta.mdx, src/content/projects/thumbnail-alpha.png, src/content/projects/thumbnail-beta.png</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Content Schema Definition, Pattern 4: Placeholder MDX, Pitfall 2: image() callback, Pitfall 4: missing thumbnails, Pitfall 5: publishDate coerce)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Content Schema Contract table -- exact field names and types)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04, D-05, D-10)
  </read_first>
  <action>
1. Create `src/content.config.ts` with the content schema. The file must contain:
   - `import { defineCollection, z } from 'astro:content';`
   - `import { glob } from 'astro/loaders';`
   - A `projects` collection using `glob({ pattern: '**/*.mdx', base: './src/content/projects' })` as the loader
   - Schema defined as a CALLBACK: `schema: ({ image }) => z.object({...})` (NOT direct z.object -- per Research Pitfall 2)
   - Fields inside z.object: title (z.string()), slug (z.string()), role (z.string()), accentColor (z.string()), thumbnail (image()), skills (z.array(z.string())), summary (z.string()), publishDate (z.coerce.date() -- NOT z.date(), per Pitfall 5), featured (z.boolean().default(false))
   - `export const collections = { projects };`

2. Create placeholder thumbnail images. Generate minimal valid PNG files (1x1 pixel or small colored rectangles):
   - `src/content/projects/thumbnail-alpha.png`
   - `src/content/projects/thumbnail-beta.png`
   Use a Node.js script to create valid PNG binary. The `image()` validator requires actual image files (per Pitfall 4).

3. Create `src/content/projects/project-alpha.mdx` (per D-04, D-05). The MDX frontmatter (between triple-dash delimiters) must contain:
   - title: "Project Alpha"
   - slug: "project-alpha"
   - role: "Lead UX Designer"
   - accentColor: "#2563EB"
   - thumbnail: "./thumbnail-alpha.png"
   - skills: ["UX Research", "Prototyping", "Figma"]
   - summary: "A placeholder project demonstrating the content schema. Replace with a real case study in Phase 3."
   - publishDate: 2024-06-15
   - featured: true
   Body: A heading "# Project Alpha" and a line stating this is placeholder content.

4. Create `src/content/projects/project-beta.mdx` with frontmatter:
   - title: "Project Beta"
   - slug: "project-beta"
   - role: "UX/UI Designer"
   - accentColor: "#7C3AED"
   - thumbnail: "./thumbnail-beta.png"
   - skills: ["User Testing", "Visual Design", "Design Systems"]
   - summary: "A second placeholder project to validate collection queries return multiple entries. Replace in Phase 3."
   - publishDate: 2024-03-10
   - featured: false
   Body: A heading "# Project Beta" and placeholder text.

5. Run `npm run build` to verify the schema validates both MDX files successfully.
  </action>
  <verify>
    <automated>cd /Users/tanyazakus/Desktop/my-portfolio && npm run build && echo "Schema validation passed"</automated>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts exists and contains `export const collections = { projects }`
    - src/content.config.ts contains `schema: ({ image }) =>` (callback form, not direct z.object)
    - src/content.config.ts contains `z.coerce.date()` for publishDate (not z.date())
    - src/content.config.ts contains `featured: z.boolean().default(false)`
    - src/content.config.ts contains `glob({ pattern: '**/*.mdx', base: './src/content/projects' })`
    - src/content/projects/project-alpha.mdx contains `title: "Project Alpha"`
    - src/content/projects/project-alpha.mdx contains `featured: true`
    - src/content/projects/project-alpha.mdx contains `thumbnail: "./thumbnail-alpha.png"`
    - src/content/projects/project-beta.mdx contains `title: "Project Beta"`
    - src/content/projects/project-beta.mdx contains `featured: false`
    - src/content/projects/thumbnail-alpha.png exists and is a valid image file
    - src/content/projects/thumbnail-beta.png exists and is a valid image file
    - `npm run build` exits with code 0 (schema validates both MDX files)
  </acceptance_criteria>
  <done>Content schema defined in content.config.ts with all 9 frontmatter fields per D-10. Two placeholder MDX files (Project Alpha, Project Beta) pass schema validation at build time. Thumbnail images exist and are resolved by the image() validator.</done>
</task>

<task type="auto">
  <name>Task 2: Create BaseLayout.astro and index.astro page</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Layout Shell Contract table, Copywriting Contract table)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: BaseLayout.astro Shell, Global CSS Import)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11, D-12, D-13, D-14, D-15)
    - src/styles/global.css (created in Plan 01 -- verify token variable names for use in layout)
  </read_first>
  <action>
1. Create `src/layouts/BaseLayout.astro`. The component script section (between the triple-dash delimiters at the top of the .astro file) must contain:
   - `import "../styles/global.css";`
   - Props interface with `title?: string` and `description?: string`
   - Destructured props with defaults: title = "Tanya Zakus -- UX/UI Designer", description = ""

   The HTML template must contain:
   - `<!DOCTYPE html>` declaration
   - `<html lang="en">` (per D-15)
   - `<head>` with: `<meta charset="UTF-8" />`, `<meta name="viewport" content="width=device-width, initial-scale=1" />` (per D-15), dynamic `<title>{title}</title>`, conditional meta description, Google Fonts preconnect links, and `<link>` loading DM Sans weights 400 and 600 (per D-06)
   - `<body class="font-sans text-[var(--color-text-primary)] bg-[var(--color-dominant)]">`
   - `<header class="sticky top-0 z-50 h-16 bg-[color-mix(in_srgb,var(--color-dominant)_95%,transparent)] backdrop-blur-sm border-b border-[var(--color-border)]">` (per D-11)
   - Inside header: `<div class="max-w-[1200px] mx-auto px-6 md:px-12 lg:px-16 h-full flex items-center justify-between">` containing:
     - `<span class="font-semibold">Tanya Zakus</span>` (site name)
     - `<nav>` with links: Work (/work), About (/about), Resume (/resume.pdf), LinkedIn (https://linkedin.com, target="_blank", rel="noopener noreferrer"). Each link has `class="min-h-[44px] flex items-center"` for 44px touch targets (per D-14)
   - `<main><slot /></main>` (per D-13)
   - `<footer class="bg-[var(--color-secondary)] py-12">` (per D-12) containing:
     - `<div class="max-w-[1200px] mx-auto px-6 md:px-12 lg:px-16">` with:
     - `<p class="text-lg font-semibold mb-4">Let's work together.</p>` (per Copywriting Contract)
     - `<a href="mailto:" class="text-[var(--color-accent)] hover:underline">Get in touch</a>` (per Copywriting Contract)
     - `<p class="mt-8 text-[var(--color-text-secondary)] text-sm">&copy; 2026 Tanya Zakus</p>`

2. Create `src/pages/index.astro` -- a minimal homepage proving BaseLayout works:
   - Component script: `import BaseLayout from "../layouts/BaseLayout.astro";`
   - Template: `<BaseLayout>` wrapping a div with `class="max-w-[1200px] mx-auto px-6 md:px-12 lg:px-16 py-16"` containing an h1 "Tanya Zakus" and a paragraph "UX/UI Designer -- Portfolio coming soon." styled with --color-text-secondary.

3. Delete any default `src/pages/index.astro` that the scaffold created (replace with ours).

4. Run `npm run build && npx astro check` to verify the layout compiles and TypeScript passes.

5. Verify `dist/index.html` contains `<header>`, `<main>`, and `<footer>` landmark regions.
  </action>
  <verify>
    <automated>cd /Users/tanyazakus/Desktop/my-portfolio && npm run build && npx astro check && grep -q '<header' dist/index.html && grep -q '<main>' dist/index.html && grep -q '<footer' dist/index.html && grep -q 'lang="en"' dist/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro exists
    - src/layouts/BaseLayout.astro contains `import "../styles/global.css"`
    - src/layouts/BaseLayout.astro contains `lang="en"`
    - src/layouts/BaseLayout.astro contains `name="viewport"`
    - src/layouts/BaseLayout.astro contains `sticky top-0 z-50`
    - src/layouts/BaseLayout.astro contains `h-16` (64px header height)
    - src/layouts/BaseLayout.astro contains `max-w-[1200px]`
    - src/layouts/BaseLayout.astro contains `px-6 md:px-12 lg:px-16` (responsive padding)
    - src/layouts/BaseLayout.astro contains `backdrop-blur-sm`
    - src/layouts/BaseLayout.astro contains `min-h-[44px]` (touch targets on nav links)
    - src/layouts/BaseLayout.astro contains `<header` (landmark)
    - src/layouts/BaseLayout.astro contains `<main>` (landmark)
    - src/layouts/BaseLayout.astro contains `<footer` (landmark)
    - src/layouts/BaseLayout.astro contains `var(--color-secondary)` on footer
    - src/layouts/BaseLayout.astro contains `py-12` on footer (48px vertical padding)
    - src/layouts/BaseLayout.astro contains `DM+Sans` in Google Fonts link
    - src/layouts/BaseLayout.astro contains `wght@400;600` in Google Fonts link
    - src/layouts/BaseLayout.astro contains `Tanya Zakus` in header
    - src/layouts/BaseLayout.astro contains `Let's work together.`
    - src/layouts/BaseLayout.astro contains `Get in touch`
    - src/pages/index.astro exists and contains `import BaseLayout`
    - dist/index.html contains `<header` after build
    - dist/index.html contains `<main>` after build
    - dist/index.html contains `<footer` after build
    - dist/index.html contains `lang="en"` after build
    - `npm run build` exits with code 0
    - `npx astro check` exits with code 0
  </acceptance_criteria>
  <done>BaseLayout.astro wraps all pages with sticky header (64px, frosted glass, 44px touch targets), main slot, and footer (secondary bg, 48px padding). index.astro renders inside BaseLayout. Build and typecheck pass. dist/index.html contains all three landmark regions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN | External CSS/font loaded from fonts.googleapis.com in BaseLayout head |
| User browser | Static HTML served to any visitor; no auth boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Google Fonts link | accept | Google Fonts is served over HTTPS; SRI hashes not standard for Google Fonts CSS (URL changes per request); low risk for static portfolio |
| T-01-04 | Information Disclosure | BaseLayout.astro | accept | No secrets, no user data; mailto: link is intentionally public; fully static public site |
| T-01-05 | Denial of Service | Google Fonts dependency | accept | If Google Fonts CDN is down, DM Sans falls back to ui-sans-serif, system-ui, sans-serif; site remains functional |
</threat_model>

<verification>
1. `npm run build` exits with code 0 -- schema validates both placeholder MDX files
2. `npx astro check` exits with code 0 -- no TypeScript errors
3. `dist/index.html` contains `<header>`, `<main>`, `<footer>` landmark regions
4. `dist/index.html` contains `lang="en"`
5. Content schema has all 9 fields per D-10
6. Both placeholder MDX files have all required frontmatter fields
</verification>

<success_criteria>
- content.config.ts validates project frontmatter with all 9 fields (title, slug, role, accentColor, thumbnail, skills, summary, publishDate, featured)
- 2 placeholder MDX files (Project Alpha, Project Beta) pass schema validation at build time
- BaseLayout.astro wraps all pages with correct header (sticky, 64px, frosted glass, 44px touch targets), main slot, and footer (secondary bg, 48px padding)
- All landmark regions present: header, main, footer
- html lang="en" and viewport meta tag included
- DM Sans loaded at weights 400 and 600 from Google Fonts
- index.astro renders inside BaseLayout and builds successfully
- Both npm run build and npx astro check pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
